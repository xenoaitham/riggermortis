# TASKS

Claim by ticking and putting your session number in brackets. Each task has
acceptance criteria and a media note (the repo films itself — every
user-visible milestone produces headless media).

## Phase 0 — Core skeleton + rig profiler
**Gate:** maps 5 varied rigs (Rigify, Mixamo GLTF, VRM, weird names, ambiguous
quadruped) with ≤2 manual corrections each, proven headless.

- [x] P0-01 [S1] Repo scaffold + STATE files + conventions + license.
- [x] P0-02 [S1] Canonical skeleton module: roles, parents, side, direction, length ratios, height bands, `rest_skeleton()`.
  - accept: acyclic, symmetric pairs, continuity test green.
  - media: review-UI ghost skeleton (later).
- [x] P0-03 [S1] Rig data model (`RigData`/`BoneData`) + JSON exchange + fingerprint.
- [x] P0-04 [S1] Name lexicon: tokenization, side detection, Mixamo trap overrides, skip tokens.
- [x] P0-05 [S1] Geometry features: directions, chain topology, proportion priors, branch/leaf analysis.
- [x] P0-06 [S1] Mapper v1: name pass (role-major greedy, side penalties) + confidence blending.
- [x] P0-07 [S1] Chain-order promotion for spine families (spine.001 / Spine1/2) + honest leftovers.
- [x] P0-08 [S1] Geometry-only fallback: root/hips disambiguation, spine-chain ordering, leg/arm/shoulder chains.
- [x] P0-09 [S1] Per-rig presets: fingerprint-keyed save/load/apply, mismatch refusal + force.
- [x] P0-10 [S1] Policy module in core (`PolicyEngine`, `Refusal`, stable codes) + docs/POLICY.md.
- [x] P0-11 [S1] CLI `rigpose`: inspect / map (—json —strict —save-preset) / preset save+load / policy status.
- [x] P0-12 [S1] Headless Blender bridge (`blender_extract.py` + `xtask/extract_blend.sh`); core stays process-free.
- [x] P0-13 [S1] Add-on skeleton registers in Blender 4.x: N-panel, inspect&map operator writing `rm_role_*` props, policy prefs, honest Phase-1 stub.
  - accept: `xtask/blender_verify.sh` end-to-end PASS.
  - media: n/a yet.
- [x] P0-14 [S1] pytest suite (49 tests): 5 synthetic rigs, determinism, presets, policy, CLI, I/O.
- [x] P0-15 [S2] Real-rig gate: Rigify meta-rig (headless generation), a real Mixamo export, a real VRM; map each ≤2 corrections; record in docs/BENCHMARKS.md.
  - done: metarig + generated Rigify (Blender 4.0.2) + Seed-san VRM all ≤2 corrections (0 each); Mixamo real export DONE S5b: Xbot.glb (real mixamorig export via three.js, no Adobe login needed) — 21/22, core complete, 0 corrections, 2 review flags; mapper fixes → DECISIONS D-007.
- [x] P0-16 [S2] CI skeleton: ruff + pytest + blender gate + **network-audit test** (zero outbound in default use).
- [x] P0-17 [S2] Review data model: `propose_reassignment()` + ambiguity ranking for the 30-second review UI.

## Phase 1 — One-image posing (the BOOM)
**Gate:** 20-image benchmark (10 photo / 10 anime), ≥90% usable-straight-away; BOOM GIF via pipeline; polish pass.

- [x] P1-1 [S2] Checksum-pinned model manager: manifest → one-time local download on explicit action, offline verify. media: n/a.
- [x] P1-2 [S3] DWPose/RTMPose ONNX wrapper (133 keypoints, CPU+GPU via onnxruntime, optional extra). accept: <2s CPU per image. — DONE: 0.85 s mean (4-figure 3060x1721), official-demo-faithful numpy re-implementation (no cv2), `rigpose detect [--json --figure N --gpu]`, 17 tests (faked sessions, no downloads).
- [x] P1-3 [S3] Multi-figure detection + figure selection data model. — DONE: `inference/figures.py` FigureBoard (stable labels, select/primary/largest), 3 tests.
- [x] P1-4 [S3] Keypoints → canonical 3D pose solve (2D→3D lift, symmetry, smoothing, elbow/knee flip disambiguation). accept: flip test on 20 poses. — DONE: pure-stdlib solver (`canonical_pose.py`, D-008), 18/20 flips correct; 2 misses are documented single-view limitations (kick, arms-back).
- [x] P1-5 [S3] FK apply engine: canonical pose → any mapped rig, rest-offset aware, undo-friendly. accept: same pose on 3 rigs passes eyeball + angle tolerance. — DONE: pure-stdlib quaternions (`fk_apply.py`), 3 real rigs at worst error 0.0000° (0.5° asserted); payload ordered (depth, name); end-to-end smoke detect→solve→FK on metarig green.
- [x] P1-6 [S4] Add-on pose UX — DONE (payload flow per D-009): Apply Pose / Clear Pose operators, panel v2 (payload picker + actionable hint, figure label + per-role confidence readout with color bands, mirror toggle), payload apply verified headlessly ≤0.5° per bone on real metarig + Seed-san (0.026°/0.020° worst). Caveats, honest: figure thumbnails deferred (a payload carries ONE figure — selection happens at `rigpose pose --figure` time); UI screenshot impossible headless (no display; documented) — media deliverable is the pipeline-rendered BOOM GIF instead.
- [x] P1-7 [S4] Viewport review overlay — DONE within the honest session scope: core data model (`review.review_items` + `skeleton_segments`, unit-tested; D-008 miss classes surface as low flip margins), add-on draw handler (gpu, confidence color bands <0.55 red / <0.75 amber / else green) registered and line data verified headlessly (16 segments, 3 bands); GPUOffScreen draw reports SKIPPED in background Blender (no GPU API — documented, not faked). Joint picking + in-viewport flip toggles + image-plane projection = P1-11.
- [x] P1-9 [S4] 20-image benchmark harness — DONE (harness + metric + docs block): `xtask/benchmark_poses.py` walks out/benchmark/images/{photo,anime}, publishes `usable-straight-away` (reliable AND flip margins ≥0.55 AND no deep-foreshortening flag) with per-image flags + confidence distribution into docs/BENCHMARKS.md between markers. Ran on what exists locally (1 photo, 1 anime — DWPose repo assets): both solve reliable (0.85/0.81 conf) but flag unmeasured flips (wrists below detection) → 0/1 usable, honestly published. 10-image sets = NEEDS-HUMAN (licensing-clean sourcing); P1-8 decision stays pending per plan.
- [x] P1-10 [S4] BOOM GIF — DONE: `xtask/render_boom.sh` = render_demos.py --demo boom (workbench turntable of the rest rig → real payload applied through the add-on's own apply path, self-check gate ≤0.5° → posed turntable; bone-proxy mesh visualizer because armature bones don't render) + assemble_gif.py (side-by-side before|after). media/boom.gif regenerated by pipeline, never committed.
- [x] P1-8 [S5] Anime-art robustness: fallback estimator plan if DWPose <90% on anime set. accept: benchmark decides. — DONE: D-011 (benchmark says <90% on both sets for decomposed reasons; fallback PLAN written, not implemented; detector no-person 2/7 anime is the anime-specific gap; n=7 of 10, re-decide at 10 — sourcing NEEDS-HUMAN). Benchmark instrument fixed first (D-010): arm kp mapping bug (wrists never observed → structural-zero margins on ALL real images), immaterial-flip auto-pass, mean flip aggregation; 18/20 fixture accept unchanged; pose-apply gate re-PASS.
- [ ] P1-8a (from D-011) Fallback estimator evaluation: sketch/anime whole-body estimator candidates, dual-estimator inference interface, per-image confidence-probe selection, `estimator` field in payload v2. Do NOT start before payload v2 (P1-11 B1) lands.
- [x] P1-9 — done 2026-09-15 S4 (see checked entry above; this stub was a stale duplicate).
- [x] P1-10 — done 2026-09-15 S4 (see checked entry above; this stub was a stale duplicate).
- [ ] P1-11 [S5] Polish pass: B1/B2/B3/B4 all done (see entries below) — task CLOSED on S5's session scope; watch P1-8a for the estimator follow-up.
- [x] P1-11 B2 [S5] Overlay interactivity: core `CanonicalPose.toggled()` (D-008 rescue, mirror-commuting, human-verified conf 1.0), `review.joint_points()` + `pick_joint()` (pure ray-cast); addon flip-toggle/pick/reset operators, joint dots, flagged-flip panel buttons; `PRIMARY_CHILD` moved to canonical.py (cycle fix). Verified: RM_REVIEW PICK/TOGGLE/TOGGLE_APPLY PASS (toggle re-apply 0.0000 deg) in pose-verify; 161 tests.
- [x] P1-11 B3 [S5] BOOM GIF framing: camera target now re-centers on the deformed bone-proxy bound-box center after payload apply (depsgraph-evaluated, exact rendered geometry); skiing pose shifted the centroid 0.345 m and the shot stays framed; boom.gif regenerated and visually checked.
- [x] P1-11 B4 [S5] UI screenshot: xvfb/display EXISTS on this box (S4 blocker gone). GENUINE windowed capture captured once and visually verified (posed metarig + review overlay in Blender 4.0.2, media/ui_screenshot.png, git-ignored). Honest caveats: windowed Blender GL on this box is flaky (~1-in-4 runs complete; crashes are GL teardown/startup, not pipeline logic — payload self-check still gates the shot); N-panel TAB not scriptable so the panel tab itself isn't in the shot (draw() is the source of record); automated re-capture is best-effort (xtask/ui_screenshot.sh).
- [x] P1-11 B1 [S5] Multi-figure payloads: core `payload.py` contract module (v2 write, v1 back-compat read, figure resolution — one implementation for addon/CLI/MCP); `rigpose pose --all-figures` embeds every figure's solve+FK; panel figure dropdown switches in-process (EnumProperty from payload, apply by label, per-figure conf readout); 9 contract tests + committed-v1-fixture back-compat test (158 total). Gate: verify_pose_apply.sh extended — 12-figure real payload, in-process switch applied at 0.0247° worst (bar 0.5°), gate PASS.

## Phase 2 — Video → animation
**Gate:** dance + fight clips playable on 3 rigs; honest side-by-side GIFs; foot-slide metric published.

- [x] P2-1 [S5] Video reader + per-frame pose detection — DONE: core `video.py` job container (deterministic stride planning, per-frame detect->solve->FK payloads, crash-safe `job.json` state after EVERY frame, resume skips done frames, plan-mismatch restarts, failures recorded honestly with re-attempt on resume); `rigpose pose-video` CLI (progress prints); decode lives in `xtask/extract_frames.sh` (ffmpeg, confined paths, D-009). LIVE-VERIFIED with real models: person video -> 5/5 reliable payloads (conf 0.70, 16 rotations); person-less frames -> honest failures; resume re-attempts failures. 7 CI tests with faked detection.
- [x] P2-2 [S5] Temporal smoothing — DONE: core `smoothing.py` — `OneEuroFilter` (Casiez 2012, adaptive cutoff via beta, timestamp-aware freq), `smooth_channel`/`smooth_pose_frames` (per-role, partial observation preserved), `reduce_keyframes` (greedy error-driven decimation sketch; endpoints kept, deterministic; P2-3 wires it into actions). 6 CI tests on synthetic jitter: variance cut >4x on stationary, fast-step tracking with beta, spike preservation, tolerance monotonicity.
- [x] P2-3 [S6] Keyframed retarget: canonical action → rig action (FK), root motion option. — DONE: core `action.py` (`load_action` through the payload contract — P2-1's writer was v2-shaped WITHOUT the `figures` list the contract requires; writer fixed, first consumer contract-clean), `condition_action` wires P2-2 smoothing + keyframe reduction (tolerance = canonical-unit joint-position error, documented); addon `bake.py` keys rotations into a NEW action per frame (mapping once, proven B=M⁻¹LM math). Root motion honestly NOT implemented (hip-anchored solve → world translation would be fabricated; documented in bake.py + BENCHMARKS.md). Gate: verify_pose_apply.sh 2-frame bake probe — pose A f1 + mirror f2, BOTH re-evaluated from fcurves: 0.0242°/0.0242° worst (16 roles each, bar 0.5°); 9 CI tests (faked payloads, stdlib).
- [x] P2-4 [S6] Foot contact detection (velocity + height heuristic). — DONE: core `contacts.py` — per-ankle speed + height-over-ground (nearest-rank percentile, self-normalizing) with Schmitt hysteresis (enter 0.02u/f & 0.08u, exit 0.06u/f or 0.20u; documented order-of-magnitude defaults, NOT fixture-fitted); gaps split intervals honestly; `attach_contacts` puts the report on the action for P2-5's IK lock. 12 CI tests: exact-interval GT match on a clean synthetic walk, P/R ≥ 0.9 with wobble, determinism, scale-field invariance, airborne → 0, hysteresis validation. PLUS P2-5's baseline instrument started: `foot_slide` metric (ankle path length in contact; 4 tests) — the ≥5× gate has a number to beat.
- [x] P2-5 [S7] Cleanup v1: IK foot lock during contact, ground-plane fit. — DONE: core `contacts.lock_feet` (per-interval ankle+toe position pin to the FIRST OBSERVED plant pose, deterministic 2-bone knee re-solve, clamps counted, `to_ground` option, bit-exact no-op on already-clean data, LockReport with slide before/after) + addon `bake_action(contacts=...)` (rig-space world pin: 2-bone ankle correction + held foot transform; `lock_dev_deg`/`lock_clamped` reported; worlds composed locally via W = P@(Mp⁻¹Mb)@B). accept MET: foot_slide 0.768u→0.000 / 0.793u→0.000 on the labeled synthetic gate (`xtask/foot_lock_gate.py`, docs block); real-Blender RM_FOOT_LOCK probe: unlocked ankle drift 0.0371m → locked 0.0000m (bar 0.010m), lock_dev 5.10°, FK fidelity unchanged (RM_BAKE 0.0242°). 10 new CI tests (217 total). Scope → D-013; numbers in docs/BENCHMARKS.md.
- [x] P2-6 [S8] Motion denoise: hip stabilization, jitter pass. — DONE: core `action.stabilize_hips` (the solve anchors hips at the origin every frame, so the surviving hip-frame noise = the per-frame scale track ["breathing"] + the anchor translation track on neck-anchored frames; centered-MA low-pass window=9 observed frames, residual damped by strength=0.7 — documented order-of-magnitude defaults, NOT fixture-fitted per D-008; each frame's correction applied as a RIGID whole-pose transform = FK-identical within the frame, so no geometry changes, no translation fabricated, stays walk-in-place; sub-snap frames bit-for-bit untouched; HipStabReport with sway before/after + max_move + honest notes). `condition_action(hip_stabilize=...)` composes stabilize → smooth → reduce in the documented order. Jitter pass = P2-2's 1€ exercised through the composition; defaults stand (no real clip justifies retuning — the S5 clip is near-static). 14 CI tests incl. the condition → detect → lock composition gate (foot-lock guarantee intact, contacts unchanged, stabilization halves breathing slide). Gate: `xtask/hip_stab_gate.py` — stance slide 0.684u → 0.339u stabilized → 0.000u locked; scale-sway RMS 0.00316 → 0.00130; drift walk still gates (0.864 → 0.000); P2-5 gate numbers untouched. Numbers in docs/BENCHMARKS.md (HIPSTAB block).
- [x] P2-7 [S8] Export: Blender actions, FBX, GLTF, VRMA (via Blender where needed). — DONE (honest scope): `xtask/export_clip.sh` + `make export-verify` — self-contained gate (no models/assets) that bakes a 2-frame action through the add-on's real bake path, exports FBX + glTF via the BUILTIN exporters (spawn lives in the shell glue, D-009), re-imports each into a fresh scene, and verifies skeleton (bone count), animation (action + fcurves across BOTH the pre-5.x and 5.x slotted-action APIs), and pose fidelity (canonical-target re-measure at both frames). Numbers on Blender 5.1.0: round-trip worst 0.0164° (glTF) / 0.0140° (FBX) vs a 2.0° bar. Wired into CI's blender-gate job. VRMA: NO builtin exporter — honestly scoped as a planned minimal writer in docs/EXPORT.md; no claims made. Gate initially FAILED and caught a real probe bug (frame 2 was measured against the mirrored pose but baked un-mirrored) — fixed, then PASS.
- [x] P2-8 [S9] Two clips × 3 rigs + side-by-side GIFs + metric in docs. media: side-by-side GIFs. — CLOSED S9 ON THE SYNTHETIC PATH, honestly decomposed (D-015): the real-clip gate ("dance + fight clips") is **NOT met with real clips** (search exhausted, NEEDS-HUMAN, out/video_smoke/SOURCES.md) and is never relabeled. What shipped: the labeled SYNTHETIC walk (same generator as the HIPSTAB numbers) retargeted to THREE real rigs (metarig .blend / Seed-san VRM / Xbot Mixamo glb) through the add-on's REAL bake path in the certified composition (stabilize -> detect -> lock; smoothing/reduction off, as the CI gate runs them); per-rig WALKRIGS block in docs/BENCHMARKS.md generated by xtask/render_walk_gifs.sh (lock ratios 21.3x/12.0x/21.1x vs the published >=5x criterion, absolute drift columns published incl. seedsan's 5% miss of the metarig-probe bar = clamp cost); 4 pipeline-generated GIFs committed under docs/media/ (walk_lock_*.gif raw|locked + walk_3rigs.gif hero) with the media-guard allowlist extended in the same commit. THIRD-RIG CATCH: Xbot's glTF tails are ~100x garbage -> bake now derives 2-bone lengths from head-to-head rest distances (metarig/seedsan gates byte-identical) + conditional tail repair in the walk pipeline + world-space proxy; -> D-015, follow-up P2-8a.
- [x] P2-8a [S9→S10] Add-on import normalization: conditional glTF/VRM bone-tail repair (same deterministic rule as xtask/walk_media.py) so add-on users posing imported rigs don't hit the evaluated-placement breakage (posed children ladder away from parents; D-015). — DONE S10, rule CORRECTED first (D-016): the 1%-disagreement trigger was NOT a no-op on sane rigs (wanted ~20 artist-intended metarig tails) — re-keyed on the measured absurd-ratio threshold (10x; sane max ~5.1x, glTF garbage >= ~76x) + co-located-child skip (span <= 5% median); addon `tails.py` and `walk_media.py` in lockstep. Wired into Inspect & Map (repairs BEFORE mapping) and session `apply_pose`, count always reported. Gate: pose-apply `RM_TAILS XBOT` PASS (raw standing-pose ankle lift 1.5177 m = ladder -> repaired 0.2817 m, inside the measured sane band 0.2464-0.3214 m from seedsan/metarig calibration; 67 bones repaired) + `RM_TAILS METARIG_NOOP` PASS (0 repaired); ALL prior gate numbers byte-identical (0.0242 deg x 3). WALKRIGS regenerated under the corrected rule: seedsan + xbot rows byte-identical, metarig 21.3x -> 21.7x (was silently repairing ~20 artist tails) — all >=5x PASS, 4 GIFs regenerated in place.

## Phase 3 — MCP server (the agent angle)
**Gate:** real MCP client, zero human Blender interaction: inspect → pose → animate → render turntable; demo GIF is a launch asset.

- [x] P3-1 [S5] Server skeleton — PARTIAL (honest): stdio JSON-RPC 2.0 (newline-delimited), initialize/server_info (protocol 2024-11-05, local_only, contract version), ping, parse-error handling. Loopback-socket transport deferred to P3-5 (session bridge) — stdio covers agent clients per mcp/DESIGN.md.
- [x] P3-2 [S5] Tool schemas v1 — DONE: 5 tools declared (inspect_rig/policy_status/map_rig LIVE; pose_from_image/animate_from_video declared with structured not_implemented answers); golden-schema test pins names/status/required fields; stdio framing test (requests, notifications, parse errors). 8 tests. Live: inspect_rig (core map), policy_status (defaults + hard lines).
- [x] P3-3 [S8] Structured policy refusals through MCP (tested like any tool). — DONE: new LIVE tool `policy_check(subject)` — refused subjects answer the EXACT `Refusal.to_dict()` shape (`code`/`message`/`category`/`retryable`, per mcp/DESIGN.md) with isError; allowed subjects answer `{"allowed": true}`; non-string subject → `invalid_request`. `policy_status` now lists the public refusal codes. Codes are IMPORTED from `riggermortis.policy` (new package exports MINOR_CONTENT/REAL_PERSON_EXPLICIT/ADULT_MODULE_DISABLED/INVALID_REQUEST — codes are public API; never re-typed in a frontend). 6 new tests: shape mirrored byte-for-byte against a fresh default engine for all 4 subjects, codes == public constants, retryable semantics (only adult_module_disabled is retryable), allowed path, golden-schema extension pins policy_check (schema v1 stays v1 — additive). 237/237 tests.
- [x] P3-4 [S9] Progress streaming for long tools. — DONE: `notifications/progress` (`progressToken`/`progress` 0..1 rounded/`phase`/`message`) emitted when `tools/call` carries `params._meta.progressToken`; without a token tools run identically and silently. `animate_from_video` is now LIVE (canonical half: load_action through the payload contract -> certified composition -> coverage + foot-slide metrics; rig-space bake honestly `not_implemented` inside the result, D-009). Capability flag flipped: `progress_streaming: true`. 4 new tests (golden schema pin, capability, end-to-end stdio streaming with a real fixture job, silent no-token path + structured error) — 241/241. mcp/DESIGN.md documents the implemented shape.
- [x] P3-5 [S10] Blender session manager: add-on ↔ server local socket, action queue. — DONE: protocol specified in mcp/DESIGN.md ("Session bridge") FIRST, then implemented: `mcp/session_bridge.py` (SessionHub queue + ledger + 127.0.0.1-only LoopbackTransport; hello/auth with required token via `hmac.compare_digest`, poll CLAIMS one-shot, dispatched actions go STALE on disconnect, never silently re-sent) + 3 new LIVE MCP tools (`session_status`/`enqueue_action`/`action_result`, schema v1 additive 6→9, golden pin updated same-commit); server starts the bridge ONLY with both `--session-port` AND `--session-token` (partial config = hard stderr error, exit 2). Add-on side: `addon/riggermortis_addon/session.py` (background client thread never touches bpy; main-thread `pump_once` executes through the REAL D-009 apply path; `bpy.app.timers` pump in interactive Blender; port+token on WindowManager = session-only) + connect/disconnect operators + N-panel section. Tests: `core/tests/test_mcp_session.py` 17 tests (queue semantics incl. deterministic ids + stale-on-disconnect + foreign-complete rejection; REAL loopback socket round-trips incl. protocol violations; disabled-bridge honesty; zero-socket default path under an audit hook WITH positive control). 261/261 tests, ruff clean. REAL-Blender E2E gate `make session-verify` (`xtask/session_verify.sh` + `session_probe.py`): agent enqueues inspect_scene + apply_pose(valid) + apply_pose(bogus) over stdio -> headless Blender's REAL add-on client claims and executes all three on the main thread -> agent collects done/done/failed with structured, actionable errors; PASS twice (stable). Wired into CI blender-gate + `make gate`.
- [x] P3-6 [S10] Example agent configs (Claude Desktop, Cursor) + docs walkthrough (5-minute connect). — DONE: `mcp/README.md` rewritten (was "not implemented yet" — false since P3-1): install -> stdio connect (zero sockets, audit-tested) -> live-Blender session quickstart (token generation in the USER's shell, both-flags rule, add-on panel connect, queue semantics one-liner), every claim citing a test or gate. Ready-made configs in `mcp/examples/` (claude_desktop_config.json, cursor_mcp.json, claude_desktop_config_session.json with the REPLACE_WITH_GENERATED_TOKEN placeholder — no usable credential literals); stale `rigpose-mcp` example in DESIGN.md fixed to the tested invocation.
- [x] P3-7 E2E agent demo run [S11]. media: **agent-driven turntable GIF** (launch asset). — DONE: bake_action + render_turntable session actions LIVE (executor: D-016 tails-first -> core.load_action through the payload contract -> certified composition min_cutoff=None -> detect -> lock -> the REAL bake with contacts -> RM_BAKE re-eval with the honest locked-chain split: FK roles bar 0.5 deg, plant-pin deviation reported separately as reeval_lock_dev_deg); new addon/turntable.py (world-composed bone proxy over MAPPED bones, FLAT + explicit world staging, depsgraph target, proportional action cycling for a clean loop, full scene restore). Gate: session_verify.sh extended to FIVE actions (adds bake_action on the xtask/walk_job.py fixture job + render_turntable) — PASS on 5.1.0: reeval 0.0210 deg <= 0.5, 59 locked frames, contacts found, slide not increased, 4 turntable frames played_action=True (CI keeps it). DEMO: make agent-demo (xtask/agent_demo.sh + agent_demo_blender.py + agent_demo_docs.py): a deterministic scripted agent over the server's stdio drove live Blender inspect(21 roles) -> pose(0.0 deg x 16 bones, real photo payload) -> animate(70 frames, slide 0.8153u -> 0.0, progress streamed) -> bake(1120 keys, 59 locked, re-eval 0.0210 deg) -> turntable(35 orbit frames) — collected via action_result; docs/media/agent_turntable.gif committed with the media-guard allowlist extension in the same commit; docs/AGENT_DEMO.md + verbatim transcript committed, SYNTHETIC motion + scripted-agent honesty stated; animate_from_video's bake field now per-process honest (session_action with the enqueue recipe on bridge-enabled servers). Wire bugs caught on the way: subshell line-counter, job built before the animate call, json repr vs JSON.
- [x] P3-8 MCP registry listing prep [S11]. — DONE (no submission — account-bound): mcp/manifest.json (name/version/local_only/transport/capabilities + the 9-tool status table) pinned by test_registry_manifest_matches_the_server_exactly (tools must match the golden schema name-for-name; version must equal SERVER_VERSION — bump together); docs/PUBLISHING.md gained the MCP registry section (what a listing may claim, each claim citing its test/gate/demo, runbook placeholder for LO's account).
- [x] P4-1 Toon shader presets [S11] (conditional — only if Phase 3 closes this session). — DONE: Phase 3 CLOSED (P3-7/P3-8), so Phase 4 OPENED. presets as DATA FILES (addon/riggermortis_addon/presets/{anime,manga,western}.json, validator fails loudly on unknown fields); addon/style.py build_toon_material = deterministic EEVEE node graph: banded diffuse (Geometry normal DOT the preset's fixed light direction -> ColorRamp with DOUBLE stops per cut = hard cel edges) + rim (Layer Weight facing -> black-to-WHITE mask ramp -> Mix swaps the rim color in — dark ink contours (manga) work the same as bright rims); UNLIT by construction (Emission strength 1, not a lit BSDF) — the look is 100% preset-determined, renders identically with no lights. Gate: make style-verify (xtask/style_verify.sh + style_probe.py) — three builds byte-deterministic (rebuild == report incl. name reclaim + in-place slot replacement), graph assertions per preset, ONE EEVEE frame per preset rendered headless on the local box (GPU boxes get real pixels; CPU-only environments answer RM_STYLE RENDER SKIPPED honestly and the graph checks still gate). Visually checked: anime 3-band blue-grey + white rim, manga 2-band ink + black contour, western 4-band warm ramp + warm rim. NOT in ci.yml yet (apt 4.0.2 would exercise only the SKIPPED path — wire deliberately per the D-014 revisit trigger). Instrument lessons: ShaderNodeMix socket access BY NAME grabs the float socket (use identifiers Factor_Float/A_Color/B_Color/Result_Color; this Blender's bracket lookup does not resolve identifiers); material slot 0 stars in renders — clear/rebind slots per preset; rebuild must REMOVE-then-create for a clean name (create-first suffixes .001).

## Phase 4 — Style system + manga maker
**Gate:** 6-page wordless manga in docs/manga/, readable + charming; same scene in 3 styles side-by-side.

- [x] P4-1 [S11] Toon shader presets (anime / manga / western cartoon) on EEVEE: banded shading, rim. — DONE (S11; full entry under Phase 3, line above — presets as data files + addon/style.py deterministic graph + make style-verify with real headless EEVEE pixels, visually checked; NOT in ci.yml yet per D-014).
- [x] P4-2 [S12] Grease Pencil line art pass. — DONE: probe FIRST answered YES with evidence (xtask/lineart_probe.py, RM_LINEART lines): Blender 5.1's GPv3 has `bpy.types.GreasePencilLineartModifier` + the `LINEART` modifier entry (legacy GPv2 class GONE — no grease_pencil_modifiers stack; Freestyle under 5.1 EEVEE draws ZERO lines and its internal Python stage crashes — dead end recorded, docs/STYLE.md); the ONLY working wiring is the ops `grease_pencil_add(type='LINEART_OBJECT')` preset (data-API builds evaluate 0 strokes; target_material assignment Blender-blocked — recorded). Builder: `addon/style.py::build_lineart` = ops-create + RENAME everything to canonical rm_* names (remove-first determinism; created object captured by DATABLOCK DIFF — bpy.context.object is stale after a render, gate-caught); `lineart` preset field = DATA (radius world-meters/opacity/contour/crease/crease_threshold/color) added to all three presets, validator extended, unknown fields still fail loudly. Gate: style-verify extended — LINEART report+determinism asserts, HARD depsgraph stroke check (strokes=1 contour on the smooth sphere), composed bands+ink EEVEE frame per preset + INK PIXELS darkened-channel check (manga 2549 / anime 2383 / western 1447, all >0; ink that renders invisibly FAILs), honest SKIPPED degradation kept. Visually checked (manga bold black contour over 2-band ink; anime/western thin outlines — silhouette-contour weight reads subtler on the gate sphere's framing, radii are per-style DATA re-tuned on real framing in P4-8). No media committed (gate frames are inspection-only), no "hand-inked" claims.
- [x] P4-3 [S12] Screentone/halftone compositor node groups; style packs as data files. — DONE: probe FIRST (xtask/tone_probe.py, RM_TONE lines — findings in docs/STYLE.md): background compositing WORKS in 5.1 but through `scene.compositing_node_group` (a node GROUP; `scene.node_tree` is GONE; sink = the group-output INTERFACE); the compositor accepts UNIFIED ShaderNode classes (Math/Mix/SeparateXYZ/ValToRGB/VectorMath — identifier-socket lesson applies) but NO combine node exists; NO UV pass under 5.1 EEVEE (Image/Alpha only) -> the coordinate source is a SECOND VIEW LAYER with material_override emitting Generated coords (probe: clean r/g gradient on the sphere); full halftone math chain PROVEN rendering (22k dark pixels, visuals checked). Builder: addon/style.py::build_screentones(scene, preset) = remove-first determinism over view layer rm_tones_uv + override material + compositor group rm_tones; dots composite OVER the image (A=render, B=ink, Fac=dist<radius mask; radius = (1-luminance)*dot_scale — operand order is load-bearing, bisect-caught). Debug catch: TWO Group Output nodes (stale unlinked first one) render BLACK — exactly one, created after the interface socket. `tones` preset field = DATA {cells, dot_scale, ink} on manga + western; anime deliberately NONE (clean cel look). Gate: TONES GRAPH PASS x2 + TONE PIXELS darkened check (manga 2327 / western 20177) + composed material+lineart+tones frames, visuals checked (western ben-day dots wrap the form; manga dots fire in its dark zones). Instrument findings recorded: node-subclass enumeration returns nothing in 5.1; ShaderNodeCombineColor is shader-only; interface gets rewritten behind the scenes and an EXISTING Group Output never refreshes.
- [x] P4-4 [S13] Multi-camera panel system + layout presets (manga RTL, western LTR). — DONE: probe-first (xtask/page_probe.py, RM_PAGE — compositor centering correction, full-page bg image, sRGB/Standard byte-identity; findings in docs/STYLE.md) then page presets as DATA (presets/pages/{manga_koma3 RTL, western_cross3 LTR}.json: rects + camera binds + gutter/bleed/border + page-level base style + per-panel style overrides) + addon/pages.py deterministic builders (render_panels / build_page_graph / render_page / remove_page; unstyled-panels-first render order so persistent style changes never leak backwards) + style-verify PAGES section (per-page px asserts, graph determinism, exposed-background/border-ring/roundtrip pixel checks, honest SKIPPED on apt 4.0.2) — STYLE GATE (P4-1..P4-4) PASS on 5.1 with real pixels, both pages visually checked incl. a genuinely mixed manga/anime page. 277 tests, lint clean.
- [x] P4-5 [S14] Speech-bubble editor (Grease Pencil + text).
- [x] P4-6 [S14] PDF/EPUB/PNG export pipeline.
- [ ] P4-7 Animatic mode from pose sequences.
- [ ] P4-8 The 6-page manga + 3-style hero shot. media: manga PDF + hero stills.

## Phase 5 — Live mode
**Gate:** 5 minutes of recorded live puppeteering; <100 ms mid-laptop.

- [ ] P5-1 Realtime ONNX pose (MediaPipe-class) in a side process.
- [ ] P5-2 Webcam → rig puppeteer with latency budget; Blender driver hookup.
- [ ] P5-3 Smoothing/latency UI + failsafe (drop to 30fps pose preview).
- [ ] P5-4 Recorded demo. media: 5-min live demo video.

## Phase 6 — Pro cleanup + optional modules
**Gate:** benchmark table published; 18+ module proven OFF by default via tests in both frontends; policy refusals fire add-on + MCP.

- [ ] P6-1 Secondary motion: hair/cloth follow-through (spring chains).
- [ ] P6-2 Motion library retarget: Mixamo/BVH/FBX → any mapped rig.
- [ ] P6-3 Public benchmark suite packaging.
- [ ] P6-4 18+ module per docs/POLICY.md (core-enforced, sober docs, no explicit content in repo).
- [ ] P6-5 Enforcement tests: fresh-install default OFF; refusal codes through add-on and MCP.
- [ ] P6-6 Optional style-LoRA trainer docs (honest GPU cost; never a dependency). No training in CI.

## Phase 7 — Launch kit
**Gate:** artist poses a rig <60s from README alone; developer connects MCP <5min from docs; CI regenerates all media.

- [ ] P7-1 README hero (BOOM GIF) + honest limitations; every claim backed.
- [ ] P7-2 Tutorials: gamedev / VTuber / webcomic / indie-animator.
- [ ] P7-3 docs/LAUNCH.md: Show-HN, BlenderNation, r/blender drafts, tweet thread, 60s video script.
- [ ] P7-4 Blender Extensions listing (manifest ready).
- [ ] P7-5 PyPI publish (needs human account) + docs.
- [ ] P7-6 60-second video cut from pipeline footage; CI media-regeneration gate. media: launch video.
