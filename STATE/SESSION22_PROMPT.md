Project (30-second context) riggermortis — local, free, rig-agnostic posing & animation engine: (1) Blender add-on, (2) MCP server so AI agents drive Blender. Drop a rigged model + reference image → pose applied. Drop a video → animation, retargeted, foot-slide-cleaned. Toon renders, manga pages. Live pose-stream puppeteering (smoothing + failsafe) shipped on the replay path. No cloud, no accounts, no uploads, ever.

Repo: /home/potato/osint/riggermortis — LIVE ON GITHUB: https://github.com/xenoaitham/riggermortis (public, MIT, branch main). Phases 0–4 CLOSED (D-017). Phase 5: P5-1/P5-2/P5-3 DONE (S17/S18/S19); P5-4 (recorded live demo + the TRUE capture→apply measurement) is the last Phase-5 piece — needs the camera. Phase 6: P6-4/P6-5 DONE in S21 (commit aec492f): the 18+ enforcement pair — addon policy binding (bpy-free), two-toggle enable through enable_adult_module(confirm=True) ONLY, verbatim refusal codes, MCP has NO enable path (D-019; D-018 stays RESERVED for the live duplicate-contract amendment), 358 tests, blender gate gained the 5/5 RM_POLICY section. Phase 6 remaining rocks: P6-1 (secondary motion / spring chains), P6-2 (motion-library retarget), P6-6 (style-LoRA trainer docs, small); P6-3 (public benchmark suite) is BLOCKED on LO's answers to the licensing question list (7 questions, in STATE/NEXT.md — a POLICY call only LO makes; do not package). Phase 7: P7-1/P7-2/P7-3 DONE in S20; P7-4/P7-5 account-bound (LO); P7-6 waits on a recorded-cut session. STEP 0 includes verifying the latest main run is green (gh run list / gh run view — S21's run was 35764633018) and, if red, downloading the log and fixing the real substance FIRST (the S12..S21 discipline).

S21's contract facts (what S22 builds on):
- The 18+ enable path is the Blender add-on preferences ONLY (two toggles: "Enable 18+ module" + "I understand the policy"), routed through enable_adult_module(confirm=True) / disable_adult_module() — never a constructor kwarg (core forbids it), never an MCP tool (D-019, test-pinned; adding one is a NEW deliberate decision). Refusal codes surface VERBATIM in the add-on report line ("refused [<code>] …"); only the retryable refusal (adult_module_disabled) carries the preferences hint.
- addon/riggermortis_addon/policy.py is bpy-free AT IMPORT by design. Core tests mount the package via conftest.addon_policy_module() (a __path__ shim that does NOT execute the bpy-importing __init__). ANY new addon module you want unit-tested the same way must stay bpy-free at import.
- bpy.context.preferences.addons.new() takes NO arguments on Blender 5.1 (the old name-argument form is dead). The real user path is addon_utils.enable(...) — that is what xtask/blender_verify.sh step 5/5 exercises (RM_POLICY ENABLE-ADDON / FRESH-OFF / ONE-TOGGLE-STILL-OFF / ENABLE-BOTH-TOGGLES / HARD-LINES-HOLD / DISABLE-REOFF; grep-tested).
- THREE claim-bearing surfaces carry the live numbers (README live bullet, docs/LAUNCH.md drafts, docs/TUTORIALS.md VTuber track) — when a number changes, grep ALL THREE in the same commit; the README status line now also says "Phase 6 opened: the 18+ enforcement pair shipped". Launch copy rules stand: every claim cites a gate/number/GIF; replay/synthetic labels verbatim; the <100 ms mid-laptop gate stays UNCLAIMED.
- The published replay budget is EMIT→APPLY: apply p95 ≈ 2.7–3.8 ms, emit→apply p50 ≈ 124–157 ms across gate runs (S18/S19/S21; jitter normal — stalls land on the producer's DETECTOR frames, the first line sits in the ~2 s cold window; never tune it away). On replayed files the envelope's age_ms is the frame file's MTIME AGE — printed informationally, never summed in. Capture→apply = age_ms + poll lag + apply, and it is UNMEASURABLE until a real stream exists.
- The WALKRIGS block is generator-owned (xtask/walk_docs.py from the out/p28 manifests) — never hand-edit; same for every BENCHMARK:* marker block. BENCHMARKS.md also carries the S21 "18+ module enforcement" block (test/gate-cited).
- Launch media (boom.gif, walk GIFs, agent_turntable.gif, ui_screenshot.png, the manga set) are pipeline-generated — reference, never hand-edit; ANY new media ships with the allowlist extension in the SAME commit + a visual check first.

STEP 0 — Session protocol (do this first, always)

CAMERA FORK FIRST — it decides the work order, one command: timeout 12 ffmpeg -hide_banner -loglevel error -f v4l2 -video_size 640x480 -i /dev/video0 -frames:v 1 out/live_probe/cam_test.png

If frames LAND (first time in eight sessions): flip the work order to P5-4 — record the live demo (xtask/live_capture.sh finally meets a streaming device; it ships UNTESTED-on-stream — debug the glue honestly, in the shell, per D-009), measure the TRUE capture→apply budget (age_ms + poll lag + apply, per-line), claim or retire the <100 ms mid-laptop gate with the measured number, publish in LIVE.md + the BENCHMARKS blocks, and update the three claim-bearing surfaces in the SAME session. The failsafe/duplicate-floor revisit trigger becomes live-relevant: if the demo shows the producer-restart suppression (fresh seq space → rig stays at rest until Stop/Start) hurts real recovery, amend the S18 duplicate contract deliberately (D-018 — the reserved number: new tests + LIVE.md amendment), never silently.
If silent again (eighth session running): work order A is P6-1, the secondary-motion opener, DESIGN-FIRST. WHY: it is the biggest remaining engine rock, it is camera-independent, and the repo's proven pattern for new subsystems is design page → probe → core build → gate (LIVE.md and STYLE.md were both written that way). Do NOT start with code: write the design page FIRST (docs/SECONDARY_MOTION.md — mechanism (spring chains over canonical roles), data model (preset/DATA fields per the presets discipline), where it hooks into the CERTIFIED composition (stabilize → detect → lock — secondary motion rides AFTER, it must never reorder or weaken the foot-lock guarantee), what is honestly out of scope (cloth simulation, collision — v1 is follow-through on hair/tail/fat-chain roles)), probe the Blender-side unknowns headlessly, then build core-side with CI tests and a gate section. D-008 discipline applies to every spring constant: order-of-magnitude defaults, declared untuned, never fitted to fixtures. P6-6 (style-LoRA trainer docs — honest GPU cost, never a dependency, NO training in CI) is the sanctioned lighter alternative if the session needs a smaller rock. P6-2 (motion-library retarget) after P6-1. P6-3 stays blocked until LO answers the question list in STATE/NEXT.md.
Then read, in order: STATE/NEXT.md (authoritative for S22 — it carries the P6-3 question list for LO), STATE/TASKS.md, STATE/PROGRESS.md (S21 entries), STATE/DECISIONS.md (esp. D-003, D-008, D-009, D-011/012, D-015/016, D-017, D-019 — all EXECUTED, don't re-litigate; D-018 is RESERVED), STATE/CONVENTIONS.md, STATE/SESSIONS.md, docs/LIVE.md (extend it if a live design changes — never fork a second live doc), docs/BENCHMARKS.md, docs/POLICY.md (now carries the as-built § Enforcement — the whole 18+ contract), and the claim-bearing surfaces docs/LAUNCH.md + docs/TUTORIALS.md. docs/STYLE.md stays the style source of record — untouched unless style behavior changes.

Register yourself in STATE/SESSIONS.md as Session 22. Claim tasks in STATE/TASKS.md by ticking + tagging [S22] before working. Append timestamped PROGRESS lines per meaningful unit (REAL UTC via date -u — do not write a clock value you did not read).

Verify baseline: cd core && /home/potato/miniconda3/bin/python3 -m pytest tests (expect 358 passed — S21 added 9) and make lint PY=/home/potato/miniconda3/bin/python3 from repo root.

Environment facts (verified through S21) — Blender 5.1.0 at /home/potato/blender-5.1.0-linux-x64/ is the REAL install. Gates need explicit env or they 127 on PATH: BLENDER=/home/potato/blender-5.1.0-linux-x64/blender, RIGPOSE=/home/potato/miniconda3/bin/rigpose, PY=/home/potato/miniconda3/bin/python3. conda BASE is the project env. onnxruntime 1.25.1 CPU provider ONLY (an RTX 3060 exists but no GPU ORT provider — the honest mid-laptop-relevant baseline); the pinned DWPose models live in ~/.local/share/riggermortis/models; local ruff 0.16.7 == CI's. The live gate needs out/live_probe/smoke_frames + out/real_rigs/metarig.rig.json (git-ignored) — without them it answers RM_LIVE GATE: SKIPPED (...) honestly, exit 0. The walk manifests live at out/p28/ (walk_docs.py --dir out/p28).

S15..S21 facts (do not reintroduce)

Never render a VSE movie from any .py (racy segfault, D-009); movie assembly is shell-glue ffmpeg + ffprobe parse-back.
Panels inherit the SCENE view transform (render_panels doesn't stage it): stage Standard + dither 0 explicitly in any page-scene.
primitive_add deselects (explicit select_set on every part + active); canonical T-pose arm pivots sit wide (narrow BEFORE hang poses); elbow bends need a pose-relative Rodrigues axis; wide panels need a 35 mm lens; pose bones default QUATERNION; a SPHERE is rotationally symmetric; Bright=0.15 changes ZERO channels (use Contrast); compositor graph = scene.compositing_node_group; GPv3 strokes via drawing.add_strokes, closed = cyclic; LineArt only via ops LINEART_OBJECT + renames; created objects captured by DATABLOCK DIFF.
Bisect verdicts need run counts (3 runs minimum per config).
Gate regexes: grep-test BOTH the PASS and SKIPPED lines locally before pushing (live-verify's SKIPPED paths: missing frames dir, missing Blender, missing rigpose, missing models — all exit 0 with RM_LIVE GATE: SKIPPED (...)). The blender gate's policy section has NO skip path — it REQUIRES Blender; grep the RM_POLICY lines if you touch the policy flow.
Mimosa intercepts bash writes of ANY source-looking file — use Write/Edit; S20/S21 also FP'd on heredocs/append pipes and commit messages whose TEXT merely NAMES source files — put appends through the Edit tool and long commit messages through a -F file. Known FP on record: "command injection" on import struct in pagedoc.py (D-003) — expect it at every commit, disclose, move on.
P5-1 live facts: envelope timing fields are MEASUREMENTS — never assert their values in tests. The FIRST stream line carries the ~2 s cold session load in detect_ms. Tracked figures carry identity from the last FULL detection; Figure.score is 0.0 on tracked figures — the quality signal is mean body confidence (17 COCO body kps, floor 0.3). kind=miss lines are NORMAL stream events; the detector CADENCE is the lever; input downscale is a dead knob.
P5-2 consumer facts: core.live.LiveTail (byte-offset tail; torn-final-line safe; producer restart detected by size-shrink OR the newline-boundary check) + core.live.LiveConsumer (latest-wins per poll; a batch ENDING in a miss applies NOTHING; envelope-seq duplicates after a replay are never re-applied; stale = no new line past stale_after, default 2.0 s). The Blender side is a THIN adapter — the ONLY bpy touches are the apply, the failsafe clear, and the readout, main-thread; the pump never raises out of the timer. The stream line IS payload-v2 through the REAL P1-6 path. The gate's RM_LIVE READY / READY-B log handshakes make budget rows measure the consumer loop, not Blender boot; producers run --idle-timeout 2; the miss run forces --conf-floor 0.95. The gate is NOT in ci.yml by design (models are local).
P5-3 facts: smoothing is CORE-side ON THE CANONICAL POSE (payload → CanonicalPose.from_dict → LivePoseSmoother.smooth(pose, t_emit_wall) → mirror → pose_apply.apply_pose_object); smoothing-OFF is byte-identical P5-2 and run A stays the UNsmoothed control. Mirror order is PROVEN (odd-symmetric 1€ filter commutes; smooth-first so a mid-session mirror toggle needs no filter-state surgery) — do not "simplify" to mirror-first. Defaults min_cutoff 1.0 Hz / beta 0.05 are DECLARED untuned (D-008) — NEVER tune them against the gate fixture; J1/J2 are gate-fed from run A's first REAL payload line, the variance bar is the P2-2 ≥4x instrument REUSED verbatim, constant-channel stillness is asserted with epsilon 1e-20 NOT ==0.0. Failsafe: stale_after keeps the last pose; sustained silence past failsafe_after (default 10 s, > stale_after core-validated) fires a ONE-TICK edge (re-armed by any new event) → driver clears to REST via pose_apply.clear_pose, resets the smoother (first recovery pose passes through EXACTLY), panel says FAILSAFE. Producer-restart-with-fresh-seq suppression is a DOCUMENTED limit (Stop/Start remedy) — revisit only via a deliberate D-018 amendment.
Do not "optimize" resolution or add smoothing strength to make numbers look better — measure, publish (D-008).

Work order (claim in this sequence; stop cleanly wherever you run out)

A. The fork from STEP 0:
   A1 (camera alive) — P5-4: record the 5-minute live demo, measure the TRUE capture→apply budget, claim or retire the <100 ms mid-laptop gate honestly, publish in LIVE.md + the BENCHMARKS blocks, update the three claim-bearing surfaces in the same session. Media rule: a demo video is NEW media → allowlist extension in the SAME commit + visual check.
   A2 (camera silent) — P6-1: secondary motion via spring chains, DESIGN-FIRST per the STEP-0 fork text above: docs page before code, probe before build, core-side module + CI tests + gate section, compose AFTER the certified chain without weakening the foot-lock guarantee, all spring constants order-of-magnitude and declared untuned (D-008).
B. P6-6 if A2 lands early: the style-LoRA trainer docs — honest GPU cost numbers, never a dependency of anything, NO training in CI, sober wording (the POLICY.md voice). Docs-only; nothing in the repo trains anything.
C. If B also lands: P6-2 opener (motion-library retarget: Mixamo/BVH/FBX → any mapped rig — probe the import paths FIRST, honest scope note on what lands in-repo vs stays a documented recipe), or deepen P6-1's gate if its design page surfaced something the probe must answer first.
D. Cheap wins while gates run
   Windowed UI screenshot attempt (best-effort ~1-in-4; S10..S21 all missed — replaces docs/media/ui_screenshot.png ONLY if genuine; never staged).
   Doc cross-checks: README status line vs TASKS/NEXT reality; the three claim-bearing surfaces vs BENCHMARKS.md (one grep sweep); AGENT_DEMO.md numbers still cite the P3-7 run (unchanged until the demo re-runs); docs/PUBLISHING.md runbooks unchanged.
   Keep docs/LIVE.md and docs/BENCHMARKS.md in sync with reality as you go.

Non-negotiables (locked, from mission + CONVENTIONS) QUALITY ABOVE ALL. Anything 80% done is 0% shipped. LOCAL OR NOTHING (zero outbound in default use; loopback 127.0.0.1 ONLY, opt-in). Rig-agnostic or fake — ambiguity reported, never swallowed. Honest claims: every claim cites a test, number, or GIF; synthetic/replay stays labeled; media rule — allowlist extension in the SAME commit as the media, visual check before shipping any image. Deterministic ops: keyed sorts, same input = same output (tested). No subprocess strings in any .py; no sockets in core (D-003); core dependency-free except lazy [inference]. Conventions: Python ≥3.10-compat, Z-up / facing −Y / character-left = +X, canonical roles frozen API, refusal codes public API, ruff + pytest green before commits (status check, not a piped tail). STATE logs append-only, timestamps REAL via date -u. Errors are actionable — (hint: ...), never trace dumps. Do NOT tune solve priors, contact thresholds, filter defaults, spring constants, or latency budgets against fixtures (D-008 — measure, publish, never fit). Do not loosen gate thresholds to move numbers. The style gate's expectation (dev box AND CI on 5.1): LINEART / TONES / PAGES / FRAMES / ANIMATIC / EXPORT PDF+EPUB: PASS + the ffmpeg assembly PASS — a SKIPPED in CI means the bump broke, not an honest degradation. 18+ module stays default-OFF; the enable path is the add-on preferences ONLY (D-019 — no MCP enable tool without a new deliberate decision); the policy spec in docs/POLICY.md is the whole contract — sober wording, no explicit content in the repo, hard lines never toggleable. Launch surfaces NEVER overstate.

End of session (non-negotiable) Tick claimed tasks in STATE/TASKS.md (completed vs partially-done with what remains). Append PROGRESS lines (real UTC via date -u). Top of STATE/NEXT.md: "NEXT SESSION SHOULD:" — write it for Session 23 (the honest fork again: P5-4 if the camera came alive mid-stream; otherwise the next rock per what A2/B actually closed — say which and why). Update STATE/SESSIONS.md row. Commit + push (routine commits authorized; CI runs — keep it green, fix inline like S12..S21: download the run log, root-cause, fix the real substance). If a Mimosa finding blocks: verify it's real, fix inline, re-scan focused, commit again — and expect the pagedoc.py import-struct FP every time, plus the heredoc/commit-message FPs when text names source files.

Known blockers (parked — do not burn time on them) Live capture device — NEEDS-HUMAN (DroidCam silent in S17..S21; re-verify at session start with the one command above; the phone side must stream). PyPI + Blender Extensions + MCP registry submissions — account-bound (LO); docs/PUBLISHING.md runbooks. P6-3 public benchmark suite — NEEDS-LO-ANSWERS (the 7-question licensing list is in STATE/NEXT.md; packaging waits). Windowed Blender GL stability — best-effort only (~1-in-4); never fake media to compensate. P1-8a fallback estimator — parked (D-011/D-012). P2-8 real walking clip — NEEDS-HUMAN (out/video_smoke/SOURCES.md).
