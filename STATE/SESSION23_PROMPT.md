Project (30-second context) riggermortis — local, free, rig-agnostic posing & animation engine: (1) Blender add-on, (2) MCP server so AI agents drive Blender. Drop a rigged model + reference image → pose applied. Drop a video → animation, retargeted, foot-slide-cleaned. Toon renders, manga pages. Live pose-stream puppeteering (smoothing + failsafe) shipped on the replay path. No cloud, no accounts, no uploads, ever.

Repo: /home/potato/osint/riggermortis — LIVE ON GITHUB: https://github.com/xenoaitham/riggermortis (public, MIT, branch main). Phases 0–4 CLOSED (D-017). Phase 5: P5-1/P5-2/P5-3 DONE (S17/S18/S19); P5-4 (recorded live demo + the TRUE capture→apply measurement) is the last Phase-5 piece — needs the camera. Phase 6: P6-4/P6-5 DONE (S21, aec492f); P6-1 DONE (S22, def156a) — secondary motion via spring chains, design-first, shipped with the RM_SECONDARY gate section and 379 tests. Phase 6 remaining rocks: P6-6 (style-LoRA trainer docs, small, docs-only) and P6-2 (motion-library retarget: Mixamo/BVH/FBX → any mapped rig); P6-3 (public benchmark suite) is BLOCKED on LO's answers to the licensing question list (7 questions, in STATE/NEXT.md — a POLICY call only LO makes; do not package). Phase 7: P7-1/P7-2/P7-3 DONE in S20; P7-4/P7-5 account-bound (LO); P7-6 waits on a recorded-cut session. STEP 0 includes verifying the latest main run is green (gh run list / gh run view — S22's P6-1 run 35773362102 completed SUCCESS, 36m6s; the prompt-file commit added one more run after it — confirm the newest is green at open, and if red, download the log and fix the real substance FIRST, the S12..S22 discipline).

S22's contract facts (what S23 builds on):
- docs/SECONDARY_MOTION.md is the design of record for secondary motion — EXTEND it if a live design changes, never fork a second secondary doc. Its Reproduce block has the as-built commands (probe / unit contract / gate).
- The pose-basis relation is PROVEN, do not re-derive or "simplify" it: pb = C @ rest @ basis with C = parent_pose @ parent_rest⁻¹ (parent_pose = parent_rest @ parent_basis for a root parent). Compose locally and carry worlds (bake.py's world_t discipline) — NEVER read pb.matrix mid-bake (stale evaluated state, the S15 trap). The probe proved it with a non-commuting roll test (rest@basis delta 0.000000 vs basis@rest 1.000000); a first probe draft got it 177°/90° wrong and the probe caught it pre-build — that is why probe-before-build is the pattern.
- core/secondary.py: ChainSpec = format-1 schema (name / anchor_role / links / rest_direction or rest_directions / freq_hz / damping_ratio), LOUD validation (unknown fields, unknown or parentless roles, bands 1–16 links, freq 0.1–20 Hz, ζ (0,5] — every refusal carries an actionable hint). presets/secondary/demo_tail.json is DATA validated through core's from_dict — one validator, no frontend re-implementations. Root/anchor role "root" is refused (no parent).
- D-008 applies to every spring constant: 3.0 Hz / ζ 0.5 / 240 Hz substep target are ORDER-OF-MAGNITUDE defaults DECLARED UNTUNED — never fit them to the gate fixture; the gate asserts behavior (respond, determ, re-eval, FK-invariance), never trajectory values.
- TWO pinned contracts in test_secondary.py: (1) root and hips are COINCIDENT in the canonical rest skeleton, so anchor orientation falls back to the primary child (hips orients as hips→spine) — a frame that still cannot orient holds + reports, never interpolates; (2) direction-only v1 is TRANSLATION-INERT (a pure translation moves the chain exactly zero) — that is a pinned test, not an accident; positional (translation-inertia) state is the DECLARED upgrade and changes the schema.
- bake_action(secondary=[(track, bone_names)]): appendage bones ONLY — validation refuses role-mapped bones, missing bones, wrong per-link count, non-parent-first chains, and chains whose first bone is not parented under the anchor role's mapped bone. Keys ride AFTER the FK roles of the same frame through the carried world_t; frames absent from a track keep the previous chain key (constant interpolation); the report carries per-chain bone/key counts under "secondary".
- The GATE section lives in xtask/verify_pose_apply.sh: RM_SECONDARY SIM / BAKE / REEVAL / FKINV, each grep-tested. There is NO combined "RM_SECONDARY: PASS" line (that is the probe's shape) — the four component greps are the gate. Current numbers: SIM max_dev 1.81° on the walk fixture, determ=True (≥0.5° respond rail — the >5° step bar lives in the PROBE, 24.84° measured; the walk is a subtle synthetic); BAKE 4 bones / 280 chain keys / 1120 FK keys over 70 frames; REEVAL 0.0000° bar 0.05; FKINV 0.00000° bar 0.001. Probe: blender -b --python xtask/secondary_probe.py (self-contained, no models).
- 379 tests (S22 added 21: core/tests/test_secondary.py). Blender gate unchanged (5/5 policy section). The three claim-bearing surfaces (README live bullet, docs/LAUNCH.md, docs/TUTORIALS.md VTuber track) were NOT touched — no live numbers changed; the README status line gained the P6-1 clause.
- Declared follow-ups, NOT owed and NOT started: secondary tracks embedded in the payload/action file format (a contract change), chain-binding presets (would extend the P0-09 per-rig preset schema), an MCP session action (schema stays v1 additive), panel UI controls, live-mode chain driving (needs the camera to measure), positional chain state.

STEP 0 — Session protocol (do this first, always)

CAMERA FORK FIRST — it decides the work order, one command: timeout 12 ffmpeg -hide_banner -loglevel error -f v4l2 -video_size 640x480 -i /dev/video0 -frames:v 1 out/live_probe/cam_test.png

If frames LAND (first time in nine sessions): flip the work order to P5-4 — record the live demo (xtask/live_capture.sh finally meets a streaming device; it ships UNTESTED-on-stream — debug the glue honestly, in the shell, per D-009), measure the TRUE capture→apply budget (age_ms + poll lag + apply, per-line), claim or retire the <100 ms mid-laptop gate with the measured number, publish in LIVE.md + the BENCHMARKS blocks, and update the three claim-bearing surfaces in the SAME session. The failsafe/duplicate-floor revisit trigger becomes live-relevant: if the demo shows the producer-restart suppression (fresh seq space → rig stays at rest until Stop/Start) hurts real recovery, amend the S18 duplicate contract deliberately (D-018 — the reserved number: new tests + LIVE.md amendment), never silently. Note: with P6-1 shipped, the live path could also drive chains — that is a DECLARED follow-up, do not bolt it onto P5-4; measure the demo first, decide separately.
If silent again (ninth session running): work order A is P6-6, the style-LoRA trainer docs, THEN the P6-2 opener. P6-6: honest GPU cost numbers (name real classes of hardware and real VRAM/compute costs; cite that the pinned DWPose inference runs CPU-only on this box), never a dependency of anything, NO training in CI, sober POLICY.md voice, docs-only — nothing in the repo trains anything. P6-2 (motion-library retarget: Mixamo/BVH/FBX → any mapped rig) follows the S22 pattern exactly: DESIGN page FIRST (docs/MOTION_LIBRARY.md — what a motion library is here, what converts, what the certified composition does to imported clips, what is honestly out of scope), then probe the import paths headlessly (Blender's FBX/glTF/BVH importers via shell glue only, D-009; Mixamo = the Xbot.glb path already gated), then build core-side with CI tests and a gate section only if the probe says the buildable half is real. Write an honest scope note on what lands in-repo vs stays a documented recipe. P6-3 stays blocked until LO answers the question list in STATE/NEXT.md.
Then read, in order: STATE/NEXT.md (authoritative for S23 — it carries the P6-3 question list for LO), STATE/TASKS.md, STATE/PROGRESS.md (S22 entries), STATE/DECISIONS.md (esp. D-003, D-008, D-009, D-011/012, D-015/016, D-017, D-019 — all EXECUTED, don't re-litigate; D-018 is RESERVED), STATE/CONVENTIONS.md, STATE/SESSIONS.md, docs/SECONDARY_MOTION.md (NEW — P6-1's design of record), docs/LIVE.md (extend it if a live design changes — never fork a second live doc), docs/BENCHMARKS.md, docs/POLICY.md, and the claim-bearing surfaces docs/LAUNCH.md + docs/TUTORIALS.md. docs/STYLE.md stays the style source of record — untouched unless style behavior changes.

Register yourself in STATE/SESSIONS.md as Session 23. Claim tasks in STATE/TASKS.md by ticking + tagging [S23] before working. Append timestamped PROGRESS lines per meaningful unit (REAL UTC via date -u — do not write a clock value you did not read).

Verify baseline: cd core && /home/potato/miniconda3/bin/python3 -m pytest tests (expect 379 passed — S22 added 21) and make lint PY=/home/potato/miniconda3/bin/python3 from repo root.

Environment facts (verified through S22) — Blender 5.1.0 at /home/potato/blender-5.1.0-linux-x64/ is the REAL install. Gates need explicit env or they 127 on PATH: BLENDER=/home/potato/blender-5.1.0-linux-x64/blender, RIGPOSE=/home/potato/miniconda3/bin/rigpose, PY=/home/potato/miniconda3/bin/python3. conda BASE is the project env. onnxruntime 1.25.1 CPU provider ONLY (an RTX 3060 exists but no GPU ORT provider — the honest mid-laptop-relevant baseline); the pinned DWPose models live in ~/.local/share/riggermortis/models; local ruff 0.16.7 == CI's. The live gate needs out/live_probe/smoke_frames + out/real_rigs/metarig.rig.json (git-ignored) — without them it answers RM_LIVE GATE: SKIPPED (...) honestly, exit 0. The walk manifests live at out/p28/ (walk_docs.py --dir out/p28).

S15..S22 facts (do not reintroduce)

Never render a VSE movie from any .py (racy segfault, D-009); movie assembly is shell-glue ffmpeg + ffprobe parse-back.
Panels inherit the SCENE view transform (render_panels doesn't stage it): stage Standard + dither 0 explicitly in any page-scene.
primitive_add deselects (explicit select_set on every part + active); canonical T-pose arm pivots sit wide (narrow BEFORE hang poses); elbow bends need a pose-relative Rodrigues axis; wide panels need a 35 mm lens; pose bones default QUATERNION; a SPHERE is rotationally symmetric; Bright=0.15 changes ZERO channels (use Contrast); compositor graph = scene.compositing_node_group; GPv3 strokes via drawing.add_strokes, closed = cyclic; LineArt only via ops LINEART_OBJECT + renames; created objects captured by DATABLOCK DIFF. EditBone uses .head/.tail (armature-space already); Bone uses .head_local/.tail_local.
Bisect verdicts need run counts (3 runs minimum per config).
Gate regexes: grep-test BOTH the PASS and SKIPPED lines locally before pushing (live-verify's SKIPPED paths: missing frames dir, missing Blender, missing rigpose, missing models — all exit 0 with RM_LIVE GATE: SKIPPED (...)). The blender gate's policy section has NO skip path — it REQUIRES Blender; grep the RM_POLICY lines if you touch the policy flow. Every new RM_ section must match its own print shape exactly (S22: the gate greps are component lines, no combined PASS).
Mimosa intercepts bash writes of ANY source-looking file — use Write/Edit; S20..S22 also FP'd on heredocs/append pipes and commit messages whose TEXT merely NAMES source files — put appends through the Edit tool and long commit messages through a -F file. Known FP on record: "command injection" on import struct in pagedoc.py (D-003) — expect it at every commit and push, disclose, move on.
P5-1 live facts: envelope timing fields are MEASUREMENTS — never assert their values in tests. The FIRST stream line carries the ~2 s cold session load in detect_ms. Tracked figures carry identity from the last FULL detection; Figure.score is 0.0 on tracked figures — the quality signal is mean body confidence (17 COCO body kps, floor 0.3). kind=miss lines are NORMAL stream events; the detector CADENCE is the lever; input downscale is a dead knob.
P5-2 consumer facts: core.live.LiveTail (byte-offset tail; torn-final-line safe; producer restart detected by size-shrink OR the newline-boundary check) + core.live.LiveConsumer (latest-wins per poll; a batch ENDING in a miss applies NOTHING; envelope-seq duplicates after a replay are never re-applied; stale = no new line past stale_after, default 2.0 s). The Blender side is a THIN adapter — the ONLY bpy touches are the apply, the failsafe clear, and the readout, main-thread; the pump never raises out of the timer. The stream line IS payload-v2 through the REAL P1-6 path. The gate's RM_LIVE READY / READY-B log handshakes make budget rows measure the consumer loop, not Blender boot; producers run --idle-timeout 2; the miss run forces --conf-floor 0.95. The gate is NOT in ci.yml by design (models are local).
P5-3 facts: smoothing is CORE-side ON THE CANONICAL POSE (payload → CanonicalPose.from_dict → LivePoseSmoother.smooth(pose, t_emit_wall) → mirror → pose_apply.apply_pose_object); smoothing-OFF is byte-identical P5-2 and run A stays the UNsmoothed control. Mirror order is PROVEN (odd-symmetric 1€ filter commutes; smooth-first so a mid-session mirror toggle needs no filter-state surgery) — do not "simplify" to mirror-first. Defaults min_cutoff 1.0 Hz / beta 0.05 are DECLARED untuned (D-008) — NEVER tune them against the gate fixture; J1/J2 are gate-fed from run A's first REAL payload line, the variance bar is the P2-2 ≥4x instrument REUSED verbatim, constant-channel stillness is asserted with epsilon 1e-20 NOT ==0.0. Failsafe: stale_after keeps the last pose; sustained silence past failsafe_after (default 10 s, > stale_after core-validated) fires a ONE-TICK edge (re-armed by any new event) → driver clears to REST via pose_apply.clear_pose, resets the smoother (first recovery pose passes through EXACTLY), panel says FAILSAFE. Producer-restart-with-fresh-seq suppression is a DOCUMENTED limit (Stop/Start remedy) — revisit only via a deliberate D-018 amendment.
P6-1 facts: the secondary binding keys chains strictly AFTER the FK roles per frame — never reorder; chain bones are appendage bones (validation refuses role-mapped ones); hold frames record held state (constant interpolation is keyed, honestly); the demo_tail.json DATA file is validated through core.ChainSpec.from_dict only.
Do not "optimize" resolution or add smoothing strength to make numbers look better — measure, publish (D-008).

Work order (claim in this sequence; stop cleanly wherever you run out)

A. The fork from STEP 0:
   A1 (camera alive) — P5-4: record the 5-minute live demo, measure the TRUE capture→apply budget, claim or retire the <100 ms mid-laptop gate honestly, publish in LIVE.md + the BENCHMARKS blocks, update the three claim-bearing surfaces in the same session. Media rule: a demo video is NEW media → allowlist extension in the SAME commit + visual check.
   A2 (camera silent) — P6-6: the style-LoRA trainer docs (honest GPU cost, never a dependency, NO training in CI, sober wording — docs-only), then the P6-2 opener: docs/MOTION_LIBRARY.md design page FIRST, probe the import paths (FBX/BVH/glTF via Blender in shell glue, D-009; the Xbot.glb Mixamo path is already gated), honest scope note, then core-side build + CI tests + gate section ONLY if the probe proves the buildable half.
B. If A2 lands both pieces early: deepen P6-2's probe into the build, or take the declared P6-1 follow-up that pays for itself first (chain-binding presets — extending the P0-09 per-rig preset schema — is the most user-visible; the others wait for the camera or a deliberate contract decision).
C. If B also lands: deepen the P6-2 gate, or P6-1's live-side design sketch in SECONDARY_MOTION.md (DESIGN ONLY, no wiring — the camera still has not existed, so nothing live-side is measurable).
D. Cheap wins while gates run
   Windowed UI screenshot attempt (best-effort ~1-in-4; S10..S22 all missed — replaces docs/media/ui_screenshot.png ONLY if genuine; never staged).
   Doc cross-checks: README status line vs TASKS/NEXT reality; the three claim-bearing surfaces vs BENCHMARKS.md (one grep sweep); AGENT_DEMO.md numbers still cite the P3-7 run (unchanged until the demo re-runs); docs/PUBLISHING.md runbooks unchanged.
   Keep docs/LIVE.md and docs/BENCHMARKS.md in sync with reality as you go.

Non-negotiables (locked, from mission + CONVENTIONS) QUALITY ABOVE ALL. Anything 80% done is 0% shipped. LOCAL OR NOTHING (zero outbound in default use; loopback 127.0.0.1 ONLY, opt-in). Rig-agnostic or fake — ambiguity reported, never swallowed. Honest claims: every claim cites a test, number, or GIF; synthetic/replay stays labeled; media rule — allowlist extension in the SAME commit as the media, visual check before shipping any image. Deterministic ops: keyed sorts, same input = same output (tested). No subprocess strings in any .py; no sockets in core (D-003); core dependency-free except lazy [inference]. Conventions: Python ≥3.10-compat, Z-up / facing −Y / character-left = +X, canonical roles frozen API, refusal codes public API, ruff + pytest green before commits (status check, not a piped tail). STATE logs append-only, timestamps REAL via date -u. Errors are actionable — (hint: ...), never trace dumps. Do NOT tune solve priors, contact thresholds, filter defaults, spring constants, or latency budgets against fixtures (D-008 — measure, publish, never fit). Do not loosen gate thresholds to move numbers. The style gate's expectation (dev box AND CI on 5.1): LINEART / TONES / PAGES / FRAMES / ANIMATIC / EXPORT PDF+EPUB: PASS + the ffmpeg assembly PASS — a SKIPPED in CI means the bump broke, not an honest degradation. 18+ module stays default-OFF; the enable path is the add-on preferences ONLY (D-019 — no MCP enable tool without a new deliberate decision); the policy spec in docs/POLICY.md is the whole contract — sober wording, no explicit content in the repo, hard lines never toggleable. Launch surfaces NEVER overstate.

End of session (non-negotiable) Tick claimed tasks in STATE/TASKS.md (completed vs partially-done with what remains). Append PROGRESS lines (real UTC via date -u). Top of STATE/NEXT.md: "NEXT SESSION SHOULD:" — write it for Session 24 (the honest fork again: P5-4 if the camera came alive mid-stream; otherwise the next rock per what A2 actually closed — say which and why). Write STATE/SESSION24_PROMPT.md (the next session's prompt, the S22→S23 pattern: updated context, the session's contract facts, STEP 0, work order, non-negotiables) and commit it. Update STATE/SESSIONS.md row. Commit + push (routine commits authorized; CI runs — keep it green, fix inline like S12..S22: download the run log, root-cause, fix the real substance). If a Mimosa finding blocks: verify it's real, fix inline, re-scan focused, commit again — and expect the pagedoc.py import-struct FP every time, plus the heredoc/commit-message FPs when text names source files.

Known blockers (parked — do not burn time on them) Live capture device — NEEDS-HUMAN (DroidCam silent in S17..S22; re-verify at session start with the one command above; the phone side must stream). PyPI + Blender Extensions + MCP registry submissions — account-bound (LO); docs/PUBLISHING.md runbooks. P6-3 public benchmark suite — NEEDS-LO-ANSWERS (the 7-question licensing list is in STATE/NEXT.md; packaging waits). Windowed Blender GL stability — best-effort only (~1-in-4); never fake media to compensate. P1-8a fallback estimator — parked (D-011/D-012). P2-8 real walking clip — NEEDS-HUMAN (out/video_smoke/SOURCES.md).
