# Sessions

| Session | Date | Scope | Outcome |
|---|---|---|---|
| 1 | 2026-09-06 | Prior-art research + name checks; repo scaffold; Phase 0 core (canonical skeleton, mapper v1, presets, policy, CLI, bridge); add-on skeleton; 49 tests; Blender gate PASS | Phase 0 ~90%: synthetic gate green, real-rig gate (P0-15) + CI (P0-16) open |
| 2 | 2026-09-06 | P0-15 real-rig gate (Rigify metarig + generated, VRM sample, edge import script, mapper fixes); P0-16 CI + network-audit test; P0-17 review data model | **Phase 0 COMPLETE**: P0-15 gate passed (metarig 0 corrections, generated 706-bone rig 22/22, Seed-san VRM 0 corrections; Mixamo NEEDS-HUMAN honestly recorded; mapper v2 → D-007), CI skeleton + audit-hook network test (84 tests), review API (`propose_reassignment`, `ambiguity_rank`, CLI `--set`), P1-1 model manager done early (real pinned sha256s, live download verified). Gate: ruff + 84/84 + blender-verify PASS |
| 3 | 2026-09-06 | P1-2 DWPose ONNX wrapper → P1-4 canonical pose solve → P1-5 FK apply engine (+ P1-3 figures) | **Phase 1 engine COMPLETE**: P1-2 wrapper 0.85 s (budget <2 s), demo-faithful numpy (no cv2), `rigpose detect` CLI; P1-4 pure-stdlib 2.5D lift, 20-pose flip accept 18/20 (2 documented misses → D-008); P1-5 FK apply, 3 real rigs at 0.0000° (tol 0.5°); P1-3 FigureBoard; end-to-end smoke detect→solve→FK on real metarig green. 128/128 tests, ruff clean |
| 4 | 2026-09-15 | P1-6 add-on pose UX (payload apply + panel v2 + mirror), P1-7 review overlay, P1-9 benchmark harness, P1-10 BOOM GIF, D-009 payload-consumer architecture | **Phase 1 UX COMPLETE**: D-009 locked (spawn lives outside .py; frontends consume payloads); `rigpose pose` one-command pipeline (live-verified, photo 0.85 + anime 0.81 conf); apply-space math derived + probed on real rigs at 0.026°/0.020° worst (bar 0.5°), mirror + clear PASS; review overlay data model + draw handler verified headlessly (GPU offscreen honestly skipped in background); benchmark harness + honest metric published (1 photo + 1 anime, NEEDS-HUMAN sourcing); BOOM GIF rendered from the real pipeline (bone-proxy visualizer). 150/150 tests, ruff clean, Phase 0 + pose-apply gates PASS |
| 5 | 2026-09-15 | Benchmark sourcing + P1-8 decision, P1-11 polish (multi-figure payloads, overlay interactivity, BOOM framing, real UI screenshot), Phase 2 opening (P2-1 video jobs, P2-2 smoothing), MCP P3-1/P3-2 skeleton | **Phase 1 CLOSED on honest numbers + Phase 2 OPENED**: benchmark sourced (10 photo + 7 anime, named Apache-2.0, SOURCES.md); instrument fixed (D-010: arm kp mapping bug — wrists never observed made flip verification structurally impossible + inflated S4 confidence; immaterial-flip auto-pass; mean flip aggregation; 18/20 fixture accept unchanged); PUBLISHED 0/10 photo + 0/7 anime usable-straight-away (detector no-person 2/7 anime) — Phase-1 ≥90% gate honestly NOT met, P1-8 → D-011 fallback-estimator PLAN (P1-8a task, not implemented). P1-11 B1–B4 done: payload v2 multi-figure (--all-figures, panel dropdown, in-process switch 0.0247° on 12-figure real payload), overlay interactivity (CanonicalPose.toggled + pure ray-cast pick_joint + addon flip/pick/reset ops, toggle re-apply 0.0000°), BOOM GIF tracks posed centroid (0.345 m shift), ONE genuine windowed UI screenshot (flaky GL documented). P2-1 video job container (resume + failure ledger, live-verified 5/5 reliable payloads on a person video; ffmpeg glue per D-009), P2-2 one-euro filter + keyframe reduction (unit-tested). P3-1/P3-2 MCP stdio skeleton (schema v1 golden tests, inspect_rig + policy_status live). 183/183 tests, ruff clean, all gates PASS |
| 7 | 2026-09-16 | P2-5 IK foot lock: core `lock_feet` (per-interval position pin + 2-bone knee re-solve) + addon bake world-pose pin (2-bone ankle correction + held foot transform) + synthetic gate instrument + RM_FOOT_LOCK Blender probe + CI pin decision | **P2-5 DONE, accept MET**: synthetic gate slide 0.768u→0.000 / 0.793u→0.000 (labeled instrument, `xtask/foot_lock_gate.py` docs block); real-Blender probe unlocked ankle drift 0.0371m → locked 0.0000m (bar 0.01m), lock_dev 5.10°, FK fidelity unchanged (0.0242°). Gate probe caught two real bugs on the way (stale `pb.matrix` once an action is assigned → local W = P@(Mp⁻¹Mb)@B composition; bone length is `Bone.length`, not the matrix 3x3). 217/217 tests, ruff clean, pose-apply + PHASE 0 gates PASS; D-013 (lock scope) + D-014 (CI keeps apt 4.0.2, documented) |
| 6 | 2026-09-16 | P2-3 keyframed retarget (canonical action model + bake-time conditioning + add-on bake + 2-frame bake gate) + P2-4 foot contact detection (hysteresis velocity/height, intervals, gap-honest) + P2-5 slide metric + P7-1 README hero start | **Phase 2 instruments LANDED**: `action.py` (job → CanonicalAction through the payload contract; video.py writer fixed to contract-valid v2 — it lacked the `figures` list), `condition_action` (1€ smoothing + keyframe reduction wired, documented params), addon `bake.py` (per-frame keyed rotations into a new action; root motion honestly unimplemented); pose-apply gate extended with the 2-frame bake check — both frames re-evaluated from fcurves at 0.0242° worst (bar 0.5°). `contacts.py` (speed+height Schmitt hysteresis, nearest-rank ground, gaps split intervals) with synthetic-walk GT: exact intervals clean, P/R ≥ 0.9 noisy; `foot_slide` metric = P2-5's ≥5× baseline. README rewritten honest (real numbers, hero = pipeline-regenerated boom.gif + genuine UI screenshot committed under docs/media/, media-guard now pins that allowlist). 207/207 tests, ruff clean, PHASE 0 + pose-apply gates PASS | **Phase 1 CLOSED on honest numbers + Phase 2 OPENED**: benchmark sourced (10 photo + 7 anime, named Apache-2.0, SOURCES.md); instrument fixed (D-010: arm kp mapping bug — wrists never observed made flip verification structurally impossible + inflated S4 confidence; immaterial-flip auto-pass; mean flip aggregation; 18/20 fixture accept unchanged); PUBLISHED 0/10 photo + 0/7 anime usable-straight-away (detector no-person 2/7 anime) — Phase-1 ≥90% gate honestly NOT met, P1-8 → D-011 fallback-estimator PLAN (P1-8a task, not implemented). P1-11 B1–B4 done: payload v2 multi-figure (--all-figures, panel dropdown, in-process switch 0.0247° on 12-figure real payload), overlay interactivity (CanonicalPose.toggled + pure ray-cast pick_joint + addon flip/pick/reset ops, toggle re-apply 0.0000°), BOOM GIF tracks posed centroid (0.345 m shift), ONE genuine windowed UI screenshot (flaky GL documented). P2-1 video job container (resume + failure ledger, live-verified 5/5 reliable payloads on a person video; ffmpeg glue per D-009), P2-2 one-euro filter + keyframe reduction (unit-tested). P3-1/P3-2 MCP stdio skeleton (schema v1 golden tests, inspect_rig + policy_status live). 183/183 tests, ruff clean, all gates PASS |
| 9 | 2026-09-17 | P2-8 close of Phase 2: synthetic-labeled clips × 3 rigs + side-by-side GIFs (headless pipeline) + per-rig metric block; P3-4 streaming if green | **PHASE 2 CLOSED (honest)**: P2-8 shipped on the synthetic path, decomposed D-015-style (real-clip gate NOT met, NEEDS-HUMAN stands): certified composition (stabilize -> detect -> lock) through the add-on's real bake on 3 rigs (metarig/VRM/Mixamo glb), lock ratios 21x/12x/21x vs the published >=5x gate, 4 pipeline-generated GIFs committed (media-guard allowlist extended same-commit). The third rig + the instrument caught 3 real bugs: bake double-support foot overwrite; glTF tails ~100x garbage (bake lengths -> head-to-head distances; conditional tail repair; world-space proxy; pose-apply gate byte-identical after); armature-local drift metric -> world units. P3-4 DONE: notifications/progress streaming (animate_from_video canonical half LIVE, bake honestly not_implemented; capability flag true; 241/241 tests, ruff clean) |
| 10 | 2026-09-17 | P3-5 Blender session manager (DESIGN.md protocol first, loopback socket mcp/+addon only, action queue) + P2-8a add-on tail normalization + P3-6 configs if green | **P3-5 + P2-8a + P3-6 DONE**: P3-5 protocol in mcp/DESIGN.md first, then `mcp/session_bridge.py` (SessionHub queue+ledger, 127.0.0.1-only transport, token-required hello, poll claims one-shot, stale-on-disconnect never re-sent) + 3 MCP tools (schema additive 6→9) + add-on client (thread never touches bpy, main-thread pump, REAL D-009 apply) + N-panel section; 261/261 tests; REAL-Blender E2E `make session-verify` PASS (agent stdio → queue → Blender executes done/done/failed with structured errors), wired into CI. P2-8a: rule CORRECTED → D-016 (absurd-ratio 10x + co-located skip; sane max ~5.1x vs garbage >= ~76x, measured); pose gate PASS (Xbot raw lift 1.52 m → repaired 0.28 m inside the calibrated sane band; metarig noop = 0; prior numbers byte-identical); WALKRIGS regen: seedsan/xbot byte-identical, metarig 21.3x → 21.7x (was silently repairing ~20 artist tails). P3-6: mcp/README 5-min walkthrough + mcp/examples/ configs + DESIGN.md example fix. UI screenshot attempt: miss (flaky GL), prior genuine capture kept. Repo-wide root ruff.toml (core standard for addon/mcp/xtask). Mimosa false-positive (SQL-injection flag on an f-string status message in the bpy add-on — no SQL exists) worked around by restructuring the edit; hook accepted |
| 8 | 2026-09-16 | P2-6 motion denoise (hip stabilization + jitter composition), P2-7 export (FBX/glTF round-trip gate), P3-3 MCP policy refusals; P2-8 prep (real-clip search) | **Phase 2 CLEANUP DONE**: P2-6 `action.stabilize_hips` (anchor-frame re-anchoring: per-frame scale "breathing" + anchor translation tracks, centered-MA window=9, strength=0.7, RIGID per-frame corrections => FK-identical within frames, no fabricated translation per D-008) composed into `condition_action` (stabilize -> smooth -> reduce); gate: stance slide 0.684u -> 0.339u stabilized -> 0.000 locked, scale-sway RMS 0.00316 -> 0.00130, drift walk still gates, P2-5 numbers untouched (separate HIPSTAB block). P2-7 `make export-verify`: bake -> builtin FBX/glTF exporters -> re-import round-trip verifying skeleton + animation (pre-5.x AND 5.x slotted-action APIs) + pose fidelity (worst 0.0164deg glTF / 0.0140deg FBX vs 2.0deg bar); wired into CI; VRMA honestly scoped (docs/EXPORT.md, no builtin exporter, no claims). P3-3: MCP `policy_check` LIVE — refusals mirror `Refusal.to_dict()` byte-for-byte, codes imported from core (new public exports), policy_status lists them, golden schema extended. 237/237 tests, ruff clean, PHASE 0 + pose-apply gates re-PASS on 5.1 (identical numbers) + export gate PASS. P2-8 prep: real-walk clip NEEDS-HUMAN (out/video_smoke/SOURCES.md) |
| 11 | 2026-09-17 | P3-7 E2E agent demo (bake_action + render_turntable session actions, extended session gate, launch GIF); P3-8 registry prep; P4-1 toon presets (Phase 4 opened) | **PHASE 3 CLOSED**: P3-7 DONE — bake_action (tails-first D-016 -> load_action -> certified composition -> real bake -> RM_BAKE re-eval 0.0210 deg <= 0.5 with honest locked-chain split) + render_turntable (addon/turntable.py: world-composed bone proxy, FLAT+world, proportional action cycling, full restore) live as session actions; session gate extended to 5 actions PASS (CI); the LAUNCH DEMO recorded (make agent-demo): scripted agent over stdio drove live Blender inspect(21 roles) -> pose(0.0 deg x 16) -> animate(70f, slide 0.8153->0.0) -> bake(1120 keys, 59 locked) -> turntable(35f), collected via action_result; docs/media/agent_turntable.gif + AGENT_DEMO.md + transcript committed (allowlist extended same-commit, SYNTHETIC + scripted-agent honesty stated). P3-8 DONE: mcp/manifest.json pinned to the golden schema by test + PUBLISHING.md registry runbook. P4-1 DONE (Phase 4 OPENED): presets as data files + addon/style.py deterministic toon graph (banded N·L ramp, Layer-Weight rim, UNLIT emission) + make style-verify with REAL headless EEVEE pixels, visually checked (anime/manga/western distinct). 264 tests, ruff clean |
| 13 | 2026-09-18 | P4-4 multi-camera panel pages (probe-first), styled turntable re-check, apply_style session action | **P4-4 DONE + animated-style claims UNLOCKED**: P4-4 probe first (RM_PAGE — caught three would-be-shipped bugs: compositor CENTERS sub-buffer images before Translate → offset correction t = desired − (page−img)//2; bare RGB node background = transparent-black outside image footprints → full-page solid image; Non-Color loads FAIL byte-fidelity → default-sRGB + Standard + dither-0 identity) then page presets as DATA (presets/pages/{manga_koma3 RTL, western_cross3 LTR}.json with page-level base style + per-panel overrides — the manga page's beat panel is anime: mixed-style showcase) + addon/pages.py deterministic builders + 14 CI tests (277 total; incl. the float-edge gutter tolerance catch 0.57−0.55 < 0.02 in binary floats). STYLE GATE (P4-1..P4-4) PASS on 5.1 with real pixels; both pages visually checked (byte-fidelity roundtrip, exposed-background/border-ring pixel checks). Work order B: styled turntable probe RM_TT STABLE PASS (LineArt re-traced on every of 12 orbit angles, tone machinery present, ink contribution measured within the same pipeline: 12335 vs 12168 baseline dark) — animated line art + tone stability now VERIFIED in docs/STYLE.md, P4-8 hero unlocked; two instrument findings recorded (compositor-free vs compositor-active AA-silhouette difference; one view transform for dark-count metrics). Work order C: apply_style session action (kinds 4→5 both sides, real P4 executors, tool schemas v1 untouched, golden test extended, DESIGN.md/README updated), SESSION GATE extended to six actions PASS on 5.1. Screenshot attempt missed (never staged). Gates: lint clean, 277 tests, media-guard clean, blender-verify + session-verify + style-verify PASS. CI red on 4.0.2 (apply_style TypeError escaping the executor's narrow catch) -> fixed inline: always-structured executor errors (the socket contract), builder capability ValueErrors, per-feature SKIPPED in the apply_style report; CI GREEN (run 35400449504) |
| 14 | 2026-09-19 | P4-5 speech bubbles (DESIGN in docs/STYLE.md → probe → schema/builders/gate), P4-6 PDF/EPUB/PNG export (stdlib writer + gate) | **P4-5 + P4-6 DONE**: P4-5 probe-first (RM_BUBBLE — GPv3 fill is a dead end for data-authored shapes: material fills only render on use_lights=False layers, stroke fill attrs never render, vertex_color drives only strokes; the body became a z-layered unlit MESH + GP ink via add_strokes/cyclic + auto-fit TEXT) then bubbles as per-panel page DATA (pages.py validator + bubble_px) + addon/bubbles.py deterministic builders (full stage+restore, compositor unset) + build_page_graph bubble compositing (strict file contract, bubble-less pages byte-identical) + gate BUBBLES RENDER/pixel checks (bubble-aware roundtrip sampling) — manga page ships a KA-BOOM! bubble (visually checked), western page gates the bubble-less path. P4-6: core/pagedoc.py pure-stdlib PDF (stdlib PNG decode all row filters → FlateDecode XObjects, no timestamps, byte-deterministic) + EPUB 3 (mimetype-first, verbatim images, content-hash id) + parse-back readers (pdfinfo-free; EPUB OPF hardened against DOCTYPE/ENTITY per Mimosa; one FP recorded: 'command injection' on import struct) + rigpose export-pdf/export-epub CLI + style-gate EXPORT section (2-page PDF/EPUB from real page renders, parse-back + byte-determinism, honest SKIPPED pre-5.x). 293 tests (+16), lint clean; STYLE GATE (P4-1..P4-6) PASS on 5.1 with real pixels; full gate battery PASS; README status refreshed. Screenshot attempt missed (never staged) |
| 15 | 2026-09-20/21 | Work order A: D-014 CI Blender bump; work order B: P4-7 animatic mode (design → probe → build → gate); docs cross-checks | **D-014 EXECUTED + P4-7 DONE**: A — ci.yml-only commit bumping CI to the official 5.1.0 tarball via actions/cache; run 35537968784 GREEN with the style gate showing LINEART/TONES/PAGES/EXPORT **PASS** (not SKIPPED) on CI (10m20s first run incl. the one-time tarball download; apt GL/numpy kept; SKIPPED degradation paths stay in code). B — probe-first (`xtask/animatic_probe.py`, RM_ANIMATIC PROBE OK): 5.1 facts bank (media_type='VIDEO' movie API; strips API w/ legacy sequences GONE; QUATERNION-rotation, symmetric-sphere, zero-crossing, and Bright=0.15 instrument traps) + the VSE MOVIE-APPEND recorded DEAD END (racy ~2/3 segfault, config-independent; single-run mitigation "passes" were statistical escapes → bisect verdicts need run counts) → the work-order fallback: deterministic per-frame PNGs through the certified bake + camera swaps (`addon/animatics.py`, pure timing plan `(k*action_len)//T`, refuses rm_page) + shell-glue ffmpeg assembly + ffprobe parse-back (D-009). Gate: `RM_STYLE ANIMATIC: PASS` (20 frames @ 12fps, two sweeps pixel-identical, shot cut 67k / swing 26k channels; the camera re-aim catch recorded) + ffmpeg half in style-verify; 12 new CI contract tests (305 total); visual check (bands follow the posed subject, cut + swing visible; TIMED ROUGH labeling everywhere); docs/STYLE.md P4-7 + README refreshed. Screenshot attempt missed (never staged). Work order C folds into S16's P4-8 start |
| 16 | 2026-09-20/21 | P4-8: 6-page wordless manga + 3-style hero (DESIGN FIRST in docs/STYLE.md → deterministic asset-free driver + 7 explicit-path page presets → 10-round visual-check loop → renders → parse-back-verified PDF), per-panel `frame` extension (pages.py + CI tests + RM_STYLE FRAMES gate), lineart radii re-tune on character framing, media-guard allowlist same-commit; work order B (ffmpeg in CI); D-017 Phase-4 close-out | **PHASE 4 CLOSED**: "Paper Dart" shipped in docs/manga/ (6 wordless RTL pages + hero_3styles.png triptych + paper_dart.pdf, all GENERATED by bash xtask/manga_build.sh; media-guard pins the 8 files, allowlist extended same-commit). 20 hand-authored canonical beats through the CERTIFIED bake + per-panel scene `frame` references (the one engine extension — staged/restored, back-compat, 6 CI tests, 311 total); the visual loop earned real findings (AgX-vs-Standard panel transform, T-pose arm pivots, Rodrigues elbow axis, 35 mm lens, one-eyed join catch) all recorded in docs/STYLE.md as-built. Work order B: ci.yml ffmpeg (4138b82) raises CI's assembly half to PASS. Full battery PASS (lint, 311, media-guard, blender-verify, session-verify, style-verify incl. FRAMES grep-tested both paths). Screenshot attempt missed (never staged). NEXT: S17 opens Phase 5 (P5-1 realtime ONNX pose) |
| 17 | 2026-09-21 | Phase 5 opener: P5-1 realtime ONNX pose side-process (docs/LIVE.md design → xtask/live_probe.py measured configs → core frame-source + timing contract + faked-detector CI tests → side-process entry + payload JSONL lines), measured latency budget vs the 0.85 s baseline | **P5-1 DONE**: probe decided the fork on numbers (REUSE pinned DWPose; detector cadence = the lever: full 550 ms / tracked-every-5 p50 88 ms / pose-only 90 ms flat; downscale a dead knob; no new model). core live.py contract + estimate_keypoints_at + rigpose live CLI + live_capture.sh glue (capture half UNTESTED: DroidCam node silent — NEEDS-HUMAN); 16 faked-detector CI tests (327 total); REAL smoke 9/9 pose lines (cold-load ~2 s on seq 0, tracked 78–120 ms); BENCHMARK:LIVE block instrument-generated. Two bugs caught by the smoke (attribute/method shadowing; total_ms scope). Capture→apply end-to-end stays P5-2's <100 ms gate, honestly not claimed |
| 18 | 2026-09-21 | P5-2's buildable half: the add-on stream consumer (DESIGN FIRST in docs/LIVE.md → core LiveTail + LiveConsumer CI-tested with fakes → addon live_driver timer pump + panel section → xtask/live_verify.sh replay gate with a REAL producer subprocess + headless Blender), replay-labeled budget | **P5-2 DONE on the buildable (replay) path**: core `LiveTail.poll()` (offset-state incremental tail; torn-final-line safe; restart-safe via size-shrink AND a newline-boundary check — the suite caught the regrow case) + `LiveConsumer` policy (latest-wins, miss-keeps-pose, duplicate-replay guard, staleness; a batch ending in a miss applies nothing — caught pre-ship by the tests; 12 faked-stream CI tests with injected clock, 339 total); addon `live_driver.py` + `rm.live_start/stop` + panel section (session-only WM props; pump never raises; apply = the REAL P1-6 path with mirror + figure); `make live-verify` gate: REAL `rigpose live` subprocess + REAL headless Blender via READY/READY-B handshakes — 9/9 applied at 0.0000° worst (bar 0.5), apply p95 3.8 ms, emit→apply p50 157 ms (stalls land on detector frames; first line in the cold window), staleness readout verified, forced-miss stream: 0 applies, pose bones byte-unchanged. Budget finding published: replay `age_ms` is mtime age → emit→apply is the honest replay number. WEBCAM half NOT met (DroidCam silent, re-verified) — <100 ms live gate UNCLAIMED, replay never relabeled. Full local battery green; camera NEEDS-HUMAN unchanged |
| 12 | 2026-09-18 | P4-2 Grease Pencil line art (probe-first), P4-3 screentones (probe-first + build), style-verify CI wiring | **P4-2 + P4-3 DONE, style gate in CI**: P4-2 probe answered the capability question with evidence — Blender 5.1 GPv3 HAS a scriptable LINEART modifier (legacy GPv2 GONE; Freestyle draws zero lines under 5.1 EEVEE and its Python stage crashes — dead end recorded); the ONE working wiring is ops LINEART_OBJECT (data-API builds evaluate 0 strokes; target_material assignment Blender-blocked) -> build_lineart = ops-create + canonical renames, remove-first determinism, datablock-diff capture (context.object is stale after a render); `lineart` preset field as DATA. P4-3 probe: background compositing WORKS via scene.compositing_node_group (node_tree GONE; unified ShaderNode classes accepted; NO combine node; NO UV pass -> second view layer with an emissive-Generated material_override as the coordinate source); build_screentones = luminance->dot-radius + coords->cell-distance + dots OVER the render; debug catches recorded (stale Group Output renders black; SUBTRACT operand order load-bearing; interface rewritten behind the scenes). Gate: STYLE GATE (P4-1+P4-2+P4-3) PASS on 5.1 with real EEVEE pixels + INK/TONE PIXELS darkened checks + visuals checked (western ben-day dots read beautifully); wired into ci.yml with honest SKIPPED halves on apt 4.0.2. docs/STYLE.md is the style source of record. 264 tests, ruff clean, full gate PASS. NOTE: the gates keep their env trap (BLENDER/RIGPOSE/PY must be explicit or they 127) |
| 19 | 2026-09-21 | P5-3: smoothing + latency/failsafe layer (DESIGN FIRST in docs/LIVE.md → core LivePoseSmoother + consumer failsafe CI-tested with fakes/clock → addon wiring + latency/smoothing panel block → live-verify extension: smoothing sweep + failsafe/recovery assertions) | **P5-3 DONE (the last camera-independent Phase-5 piece)**: design of record first (smoothing wire point on the CANONICAL POSE core-side; mirror order PROVEN — odd-symmetric filter commutes, smooth-first for state stability; t_emit_wall time base with gap-snap semantics; defaults declared untuned D-008 starting points; failsafe = sustained silence → clear-to-REST with edge-in-core/action-in-adapter). Core LivePoseSmoother + LiveConsumer failsafe (one-tick edge, latch, re-arm, >stale_after validated), 10 CI tests (349 total, incl. mirror commutation + injected-clock failsafe). Addon: smoothed apply path (OFF byte-identical P5-2), failsafe clear-to-rest, panel props + latency/smoothing readouts. live-verify extended: run A control reproduces S18 (9/9 at 0.0000°), jitter sweep variance cut 8.5x vs the P2-2 ≥4x bar (mean tracks, constant channels still — an FP-epsilon instrument lesson caught pre-ship), failsafe/rest/recovery PASS; RM_LIVE lines grep-tested both paths. Full battery green (7 gates + lint + 349). Camera silent again → P5-4 stays blocked; NEXT = launch kit (P7-1/P7-3) as the honest fork |
| 20 | 2026-09-22 | Launch kit work order A (camera silent 5th session — ffmpeg probe timeout, no packets): P7-1 README hero refresh + honest limitations; P7-3 docs/LAUNCH.md drafts (Show-HN, BlenderNation, r/blender, tweet thread, 60s script); work order C (P7-2 TUTORIALS.md) when A+B landed early | **LAUNCH KIT LANDED**: P7-1 DONE (README status to the Phase-5 truth; live-mode bullet with measured numbers — 550/88/90 ms side-process, consumer 9/9 at 0.0000°, apply p95 2.7–3.8 ms, emit→apply 124–157 ms across gate runs, sweep 8.5×/failsafe — replay labels verbatim, <100 ms UNCLAIMED; agent turntable + manga hero embedded by reference; limitations refreshed incl. the tails bullet corrected to the D-016 shipped reality). P7-3 DONE (five drafts, every claim cited, live half labeled, 60s script cuts only existing footage — P7-6's shot list). P7-2 DONE (docs/TUTORIALS.md, four tracks, real commands verified against cli.py + registered operators, gaps named in GAP: lines). Cheap wins: WALKRIGS stale D-015 text fixed via the generator + regen (rows byte-identical); BENCHMARKS/LIVE gained the S19 budget re-run note; UI screenshot attempt missed (6th, never staged); media-guard clean. Phase 7 remains: P7-4/P7-5 (account-bound), P7-6 (cut session). NEXT: camera fork → P5-4, else P6-4/P6-5 enforcement pair |
| 21 | 2026-09-22 | Camera silent a 7th session (ffmpeg timeout, exit 124, no packets) → work order A2: P6-4 (the 18+ module per docs/POLICY.md) + P6-5 (enforcement tests through both frontends) | **POLICY ENFORCEMENT PAIR LANDED**: P6-4 DONE (addon/riggermortis_addon/policy.py — bpy-free binding, engine only via the default path, two-toggle sync through enable_adult_module(confirm=True)/disable; verbatim refusal codes in the add-on report; rm.policy_check operator + panel section + register() prefs sync; MCP deliberately gains NO enable path — D-019, D-018 stays reserved for the live amendment). P6-5 DONE (9 new tests, 358 total: strict confirm, fresh-install OFF via the binding + MCP no-enable-tool test, verbatim codes, network-audit sweep extended; blender_verify.sh 5/5 policy probe via addon_utils.enable — the real checkbox flow — RM_POLICY lines grep-tested, PASS). Battery: lint clean, 358 passed, media-guard clean, blender/export/session/pose/style/live gates PASS. Docs: POLICY.md as-built + Enforcement section, README policy paragraphs, BENCHMARKS enforcement block. NEXT: camera fork again → P5-4, else P6-1 opener (design-first spring chains) with the P6-3 licensing question list ready for LO |
| 23 | 2026-09-22/23 | Camera silent a 10th session (ffmpeg exit 124, no packets) → work order A2: P6-6 style-LoRA trainer docs (docs-only, honest GPU cost) then P6-2 opener (motion-library retarget, DESIGN FIRST docs/MOTION_LIBRARY.md → import-path probe → core build only if proven) | **P6-6 DONE + P6-2 CORE DONE**: P6-6 = docs/STYLE_LORA.md (docs-only: input-side recipe, honest community-labeled GPU cost table, never-a-dependency list, a Reproduce block of provable nothings — all verified). P6-2 = DESIGN FIRST (positions-only conversion, hips-anchored, rest-span scale, walk-in-place, certified composition untouched — mocap foot-slide cleaning for free) → probe ALL PASS (RM_MOTION: BVH/FBX round-trips 0.000000 canonical-units; EXTRACT-ROUNDTRIP 0.0000° via sample→canonical→fresh target; ROOT-DROP within FP epsilon; DETERM byte-identical) with FIVE would-be-shipped bugs caught pre-build (posed head = pb.matrix.to_translation(); positions are the only convention-free metric — BVH reverses axes; BVH import needs axis_forward='Y', axis_up='Z' — defaults 90°, '-Y' MIRRORS; sample before any rotation-mode change; retarget applies to the TARGET's topology) + the finding that **Xbot.glb carries SEVEN real Mixamo clips** (walk/run/idle/…, a REAL animated fixture, no Adobe login) → core motion.py (MotionClip format-1 loud validation + pose_from_sample + action_from_clip) + 27 CI tests incl. the certified-composition contract pinned core-side on a sliding-walk clip (**406 passed**, lint clean). Cheap win + LO catch: ui_screenshot.sh had been launching the BROKEN apt 4.0.2 (bare `blender` on PATH) — fixed to resolve $BLENDER→5.1→PATH + timeout --kill-after=10; miss #10 stands, on 5.1, never staged. S24 = bridge sampler + fixtures + RM_MOTION gate + the Xbot walk REAL-Motion row |
| 22 | 2026-09-22 | Camera silent an 8th session (ffmpeg exit 124, no packets) → work order A2: P6-1 opener, DESIGN-FIRST (docs/SECONDARY_MOTION.md → probe → core module + CI tests → addon bake wiring + RM_SECONDARY gate section), riding AFTER the certified composition, D-008 constants | **P6-1 DONE (design → probe → build → gate, in that order)**: docs/SECONDARY_MOTION.md written BEFORE code; the probe caught the pose-basis composition bug pre-build (pb = C @ rest @ basis proven by a non-commuting roll test 0.000000 vs 1.000000) then PASSED everything (COMPOSE/REEVAL/COMPOSE-XFORM 0.0000°, FOLLOW 24.84°, SETTLE 0.00°, DETERM byte-identical). Core `secondary.py` (ChainSpec format-1 loud validation + presets/secondary/demo_tail.json DATA; deterministic integer-substep Euler sim, never mutates the action; translation-inertness PINNED as the direction-only v1 contract; hips→spine fallback for the coincident root/hips joints) + 21 CI tests (379 total) + `bake_action(secondary=…)` binding (appendage bones only, strictly after FK roles) + RM_SECONDARY gate section (SIM 1.81°/determ, BAKE 4/280/1120, REEVAL 0.0000°, FKINV 0.00000°). Battery green (lint, media-guard, 379, blender/export/session/pose/style/live). Screenshot attempt MISS (9th). S21's CI run confirmed SUCCESS (35764633018); S21-close leftovers folded into S22's commits per the no-race note. NEXT: P5-4 if the camera lands, else P6-6 docs + P6-2 opener |
